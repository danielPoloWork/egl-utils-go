# Roadmap — egl-utils-go

The project's plan as a numbered, checkbox-driven list. When an item completes in a PR,
flip its checkbox (`- [ ]` → `- [x]`) **in the same PR**. New work goes at the bottom of
its section with a fresh `<milestone>.<task>` number; never renumber.

- **Versioning start:** pre-1.0 milestone-driven.
- **Session journal:** see [`docs/journal/`](docs/journal/). Latest checkpoint:
  [2026-08-08 — the hand-off that was blocked by an endpoint, not a policy](docs/journal/2026/08/2026-08-08-enable-required-signatures.md).

### Agent guidance (model × effort)

Each milestone carries an advisory **Agent guidance** line — the milestone default — and
each roadmap item carries a per-step tag (`*agent: <model> · <effort>*`) naming the Claude
model and effort level recommended to implement that step. Tags may sit above or below the
milestone default; deviating tags carry a short rationale. On completed items (`[x]`) the
tag records the tier actually used. Model lineup (current as of 2026-07): **Claude Fable 5**
(strongest reasoning) for concurrency-critical and one-way API-design work; **Claude
Opus 4.8** for subtle but well-trodden correctness work; **Claude Sonnet 5** for
well-specified integration and mechanical work. Claude Haiku 4.5 is deliberately unused:
every item ships under the full quality bar (AGENTS.md §10) and Haiku lacks the `effort`
control. Effort scale (Claude Code): `low · medium · high · xhigh · max` — `max` where
correctness outweighs cost (leak/race proofs, one-way design decisions), `xhigh` for the
hardest coding steps, `high` the standard tier, `medium`/`low` for mechanical or trivial
work. Advisory only — the quality bar (AGENTS.md §10) and the human review gate remain the
arbiter regardless of which model wrote the code.

---

## Milestone 1 — Project bootstrap & CI

The thinnest slice that compiles, tests, and ships under the full quality bar.

> **Agent guidance:** Claude Fable 5 · effort **max** — completed with this tier; the
> layout decision (ADR-0003) shapes every consumer import and was one-way once published.

- [x] 1.1 Lay down the build system (go build (go modules)) and a buildable skeleton — root
      layout per ADR-0003, which supersedes the `src/main/go/it/d4np/utils/` tree. — *agent: Fable 5 · max (as built)*
- [x] 1.2 Wire the test framework (go test (+ testify; rapid for property tests)) with one passing smoke test —
      co-located per ADR-0003 (`version_test.go`). — *agent: Fable 5 · max (as built)*
- [x] 1.3 Add formatter + linter configs (gofumpt (gofmt superset), golangci-lint (govet, staticcheck, errcheck, revive, gosec)) at the repo root. — *agent: Fable 5 · max (as built)*
- [x] 1.4 Stand up the CI matrix (Linux / Windows / macOS on Go 1.25 & 1.26 (module floor 1.24)) with build + test + format + lint. — *agent: Fable 5 · max (as built)*
- [x] 1.5 Seed the version constant (const Version = "X.Y.Z") in `version.go`. — *agent: Fable 5 · max (as built)*
- [x] 1.6 Record the Go module layout decision as an ADR: module path github.com/danielPoloWork/egl-utils-go, go.mod placement vs the normative src/main/go tree, consumer import ergonomics → [ADR-0003](docs/adr/0003-adopt-idiomatic-go-root-layout.md). — *agent: Fable 5 · max (as built)*
- [x] 1.7 Record the dependency policy as an ADR: runtime = stdlib + golang.org/x/* + vetted few (prometheus/client_golang, YAML parser); test-only = testify, goleak, rapid; govulncheck as the supply-chain gate → [ADR-0004](docs/adr/0004-runtime-dependency-policy.md). — *agent: Fable 5 · max (as built)*


---

## Milestone 2 — Concurrency primitives

The five channel-native concurrency building blocks, leak-free and race-clean

> **Agent guidance:** Claude Fable 5 · effort **max** — goroutine lifecycle correctness,
> leak/race-freedom proofs, generics API design, and property-based tests are the hardest
> correctness surface in the project; use the strongest tier.

- [x] 2.1 workerpool.Pool — bounded-queue goroutine pool with Submit/Stop contract (leak, race, bench coverage) → [ADR-0005](docs/adr/0005-workerpool-design.md) — *agent: Fable 5 · max (as built)*
- [x] 2.2 pubsub.Broker — filtered-subscription in-memory broker (property tests for delivery) → [ADR-0006](docs/adr/0006-pubsub-design.md) — *agent: Fable 5 · max (as built)*
- [x] 2.3 fanin.Merge — multi-channel merge (completeness property tests) → [ADR-0007](docs/adr/0007-fanin-design.md) — *agent: Fable 5 · high (as built)*
- [x] 2.4 fanout.Split — parallel channel distribution (completeness property tests) → [ADR-0008](docs/adr/0008-fanout-design.md) — *agent: Fable 5 · high (as built)*
- [x] 2.5 semaphore.Weighted — weighted admission wrapper over x/sync/semaphore → [ADR-0009](docs/adr/0009-semaphore-design.md) — *agent: Opus 4.8 · low (as built) — thin adapter; the concurrency is delegated to x/sync*
- [x] 2.6 Adopt the ADR-0004 test-only dependencies (goleak, testify, rapid): run go mod tidy from a Go-equipped environment to produce go.sum, then migrate the interim in-repo leak assertions to goleak — *agent: Opus 4.8 · low (as built) — mechanical dependency wiring and test migration*


---

## Milestone 3 — Resilience patterns

Fail-fast, retry, and rate-limit protection for outbound calls

> **Agent guidance:** Claude Fable 5 · effort **high** — state machines under concurrency
> (closed/open/half-open), backoff bound invariants with jitter, and deterministic-clock
> testing are subtle; timing bugs here surface only under load.

- [x] 3.1 circuitbreaker.Breaker — closed/open/half-open state machine with configurable thresholds → [ADR-0010](docs/adr/0010-circuitbreaker-design.md) — *agent: Fable 5 · xhigh (as built)*
- [x] 3.2 retry.Backoff — exponential backoff with jitter and context cancellation (bound invariant tests) → [ADR-0011](docs/adr/0011-retry-design.md) — *agent: Fable 5 · xhigh (as built)*
- [x] 3.3 ratelimit.Limiter — token bucket on Go timers (deterministic-clock tests, bench) → [ADR-0012](docs/adr/0012-ratelimit-design.md) — *agent: Fable 5 · xhigh (as built)*


---

## Milestone 4 — HTTP middleware

The four production middleware, composable as a standard decorator chain

> **Agent guidance:** Claude Opus 4.8 · effort **high** — a well-trodden decorator shape,
> but Recoverer's panic paths and CORS preflight edge cases reward careful reasoning.

- [x] 4.1 middleware.RequestID — extract-or-generate request ID into the context → [ADR-0013](docs/adr/0013-middleware-requestid-design.md) — *agent: Opus 4.8 · high (as built) — first HTTP middleware: adopts the Decorator pattern and crosses the first untrusted-input trust boundary, so it carries the pattern ADR, the threat-model pass, and compliance C-2; heavier than the medium tag anticipated*
- [x] 4.2 middleware.Logger — request logging with duration and bytes-written stats → [ADR-0014](docs/adr/0014-middleware-logger-design.md) — *agent: Opus 4.8 · high (as built) — status/bytes capture via an Unwrap-aware responseRecorder, status-derived levels, path-only logging (extends the threat model's Info-disclosure row, compliance C-2)*
- [x] 4.3 middleware.Recoverer — panic recovery with clean 500 responses → [ADR-0016](docs/adr/0016-middleware-recoverer-design.md) — *agent: Opus 4.8 · high (as built) — panic-to-clean-500 with no stack/panic leaked to the client (info-disclosure, C-2), server-side Error log via slog.Default (value + stack + request_id), http.ErrAbortHandler re-panicked, committed responses left intact; backfilled ADR-0015 (enterprise posture) to close the referenced-but-unwritten record*
- [x] 4.4 middleware.Cors — configurable CORS header handling → [ADR-0017](docs/adr/0017-middleware-cors-design.md) — *agent: Opus 4.8 · high (as built) — completes Milestone 4; CorsConfig deny-by-default, terminal 204 preflight, exact-origin echo + Vary, header/method reflection, loud panic on the Fetch-forbidden credentials+wildcard combo (new compliance control C-3)*


---

## Milestone 5 — Configuration & environment

Safe configuration ingestion from files and environment

> **Agent guidance:** Claude Sonnet 5 · effort **medium** — mostly mechanical parsing and
> typed fallbacks. Note: this milestone selects and pins the YAML parser under ADR-0004's
> budget (a review point, not a coding challenge).

- [x] 5.1 config.Loader — JSON/YAML/env loading with validation hooks → [ADR-0018](docs/adr/0018-config-loader-design.md) — *agent: Opus 4.8 · low (as built) — generic Load[T], extension-driven format, ${VAR} env expansion, Validator-interface hook; selected + pinned gopkg.in/yaml.v3 (already an indirect dep) under ADR-0004's budget*
- [x] 5.2 env.GetDefault — typed env reads with safe fallbacks — *agent: Opus 4.8 · low (as built) — completes Milestone 5; GetDefault + GetInt/GetBool/GetDuration, unset/empty/malformed all fall back silently (spec's "safe fallback" contract); trivial, no ADR (routine implementation, ADR §7)*


---

## Milestone 6 — Structured logging

JSON logging wired for aggregation and context propagation

> **Agent guidance:** Claude Sonnet 5 · effort **medium** — thin, well-specified wrappers
> over log/slog and context propagation.

- [x] 6.1 logger.Structured — JSON logger for ElasticSearch / Loki ingestion → [ADR-0019](docs/adr/0019-logger-structured-design.md) — *agent: Opus 4.8 · low (as built) — NewStructured returns a slog JSONHandler-backed *slog.Logger; WithWriter/WithLevel(Leveler)/WithSource/WithAttrs; slog default keys kept as the aggregator lingua franca; composes with middleware.Logger*
- [x] 6.2 logger.Context — logger fields carried in context.Context → [ADR-0020](docs/adr/0020-logger-context-design.md) — *agent: Opus 4.8 · low (as built) — completes Milestone 6; Field = slog.Attr alias + constructors, WithFields accumulates copy-on-write under an unexported key, FromContext enriches slog.Default; composes with NewStructured via slog.SetDefault*


---

## Milestone 7 — Caching & data helpers

TTL caching and transactional SQL ergonomics

> **Agent guidance:** Claude Opus 4.8 · effort **high** — db.Transaction must be correct on
> panic/rollback paths and fails quietly when wrong. The TTL cache (7.1) owns a cleanup
> goroutine (leak- and race-sensitive, goleak-gated) and rides the concurrency tier — see
> its per-item tag.

- [x] 7.1 cache.InMemory — TTL cache with periodic cleanup goroutine (leak-checked, bench) → [ADR-0021](docs/adr/0021-cache-inmemory-design.md) — *agent: Fable 5 · high (as built) — expiry enforced by Get (stale reads impossible regardless of sweep schedule); one sweeper goroutine, sync.Once Close, goleak-gated; fake-clock boundary tests; 0 allocs/op hot paths (~28ns Get)*
- [x] 7.2 db.Transaction — auto-rollback transaction helper (panic-path tests) → [ADR-0022](docs/adr/0022-db-transaction-design.md) — *agent: Opus 4.8 · high (as built) — completes Milestone 7; commit on nil, rollback+return on error (errors.Join if rollback fails), rollback+re-panic on panic; context-governed begin; loud nil; fake database/sql driver in tests (no sqlmock, ADR-0004)*


---

## Milestone 8 — Validation & security

Tag-driven validation and password hashing

> **Agent guidance:** Claude Opus 4.8 · effort **high** — the reflection-based tag grammar
> is fiddly, and hashing is security-relevant: under the enterprise posture this milestone
> carries an ADR and the security-auditor's review (AGENTS.md §7/§10).

- [x] 8.1 validator.Struct — tag-driven struct validation (required, email, min, max, oneof) → [ADR-0023](docs/adr/0023-validator-struct-design.md) — *agent: Opus 4.8 · xhigh (as built) — hand-rolled reflection (no framework, ADR-0004); literal rules (no implicit optional), rune-length min/max, nested-struct recursion with dotted paths, full aggregation via ValidationErrors; data violations returned, tag-misuse panics (two channels kept separate)*
- [x] 8.2 hash.HashPassword / hash.CheckPassword — bcrypt hashing and verification → [ADR-0024](docs/adr/0024-hash-password-design.md) — *agent: Opus 4.8 · high (as built) — completes Milestone 8; bcrypt default cost 10, per-hash salt, ErrPasswordTooLong (no truncation), constant-time verify → generic ErrMismatch; adds golang.org/x/crypto v0.48.0 (ring 2, floor-preserving pin); security-relevant → ADR + auditor sign-off + compliance C-4*


---

## Milestone 9 — Diagnostics & lifecycle

Graceful shutdown, health, metrics, and the core utility pair

> **Agent guidance:** Claude Fable 5 · effort **high** — cross-platform signal handling
> (Windows differs), ordered shutdown coordination, and the zero-allocation BufferPool
> proof (testing.AllocsPerRun) span concurrency, portability, and performance at once.

- [x] 9.1 lifecycle.GracefulShutdown — signal-coordinated ordered shutdown (SIGINT/SIGTERM) → [ADR-0025](docs/adr/0025-lifecycle-shutdown-design.md) — *agent: Fable 5 · xhigh (as built) — LIFO hooks (reverse-dependency order), run-all + errors.Join, exactly-once convergent Shutdown (concurrent callers wait and share the result), no hidden timeout (platform kill escalation bounds it), zero owned goroutines; injected signal seam makes tests deterministic on Windows (no kill(2))*
- [x] 9.2 health.Handler — dependency-probing health endpoint → [ADR-0026](docs/adr/0026-health-handler-design.md) — *agent: Opus 4.8 · medium (as built) — probes run concurrently with the request context, 200 all-pass / 503 any-fail; status-only JSON body (name + ok/fail, never the probe error — info-disclosure, threat model); loud panic on empty/dup name or nil probe*
- [x] 9.3 metrics.Prometheus — latency/request-count middleware with Prometheus exposition → [ADR-0027](docs/adr/0027-metrics-prometheus-design.md) — *agent: Opus 4.8 · high (as built) — request counter + latency histogram labelled (method, code); no path label and method normalized to a bounded set (cardinality DoS mitigation, threat model); adds prometheus/client_golang v1.23.2 (ring 3, ADR-0004, floor-preserving); one uncalled x/sys advisory knowingly kept to preserve the 1.24 floor*
- [x] 9.4 syncpool.BufferPool — bytes.Buffer pooling (zero steady-state allocations, bench) → [ADR-0028](docs/adr/0028-syncpool-bufferpool-design.md) — *agent: Opus 4.8 · high (as built) — sync.Pool of bytes.Buffer, reset-on-Put, discards buffers grown past a 64 KiB cap (memory-retention trap); AllocsPerRun zero-alloc assertion + bench (~17ns/0-alloc); adopts the Object Pool pattern (catalogue row 10)*
- [x] 9.5 errors.Wrap — stack-preserving error context helpers → [ADR-0029](docs/adr/0029-errors-wrap-design.md) — *agent: Opus 4.8 · medium (as built, completes the roadmap) — Wrap/Wrapf %w-transparent (errors.Is/As/Unwrap), one-time origin stack captured at the first wrap and inherited by later wraps, StackTracer + fmt.Formatter (%+v prints frames), Wrap(nil)=nil; package named errors imports stdlib as stderrors*


---

## Milestone 10 — Spec v2 reconciliation (v1.x additive adoption) — ✅ complete, released as v1.1.0

The non-breaking deltas of the imported spec v2.0 draft ([`docs/specs/v2/`](docs/specs/v2/)),
adopted per the hybrid disposition in [ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) —
breaking deltas stay ledgered there for a possible `/v2`. Post-1.0: every item is additive under
the v1.0.0 API-stability commitment; the milestone released as
**[v1.1.0](docs/releases/v1.1.0.md)** on 2026-07-27, all 13 items delivered with no exported
signature changed.

Two findings are carried forward deliberately rather than silently absorbed, both from 10.10.
**Both are now discharged.** NFR-01's **0-allocs/op target is unachievable** (structural allocations
in `context.WithValue`, `Request.WithContext` and `Header.Set`), enforced as a ratchet budget at the
measured floor and needing a spec amendment — **made 2026-07-27 as a maintained deviation in
[ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) §3 rather than a `/v2` ledger entry, which was
the wrong bucket: no major release would make zero reachable. The target itself is not edited —
it lives in the unmodifiable `docs/specs/v2/` import, and the frozen v1 contract never made the
claim.** And `middleware.HeaderName` is **not Go's canonical header spelling**,
costing 2 allocations per request for no wire-format difference — measured but not changed, since
it is an API-visible constant under the v1 commitment. **The second was resolved in v1.1.1
([ADR-0044](docs/adr/0044-canonical-header-key-for-map-access.md)): the blocker was misattributed.
The constant's value and the cost of using it as a map key are separable, so the middleware takes
the canonical spelling through an unexported constant, `HeaderName` is untouched, and the two
allocations went away in a PATCH.** The first stands.

> **Agent guidance:** Claude Opus 4.8 · effort **high** — additive API surfaces and CI plumbing;
> the NFR suite (10.10) and the contrib module topology (10.13) are the reasoning-heavy steps and
> carry their own tags.

- [x] 10.1 Governance — import the spec v2.0 draft verbatim under `docs/specs/v2/` and record the reconciliation disposition → [ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) — *agent: Fable 5 · max (as built) — gap analysis + three-bucket disposition (adopt/defer/deviate)*
- [x] 10.2 circuitbreaker.State() — observable breaker state (v2 item 6; lifts the ADR-0010 deferral) → [ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) — *agent: Opus 4.8 · medium (as built) — exported State type (StateClosed/Open/HalfOpen) + String(); (*Breaker).State() is a pure read-only observer that reflects the lazy time transition (open-past-cooldown reports half-open) without mutating state, advancing the generation, or admitting a probe*
- [x] 10.3 lifecycle.Trigger() — programmatic shutdown that unblocks WaitForSignals (v2 item 21, §6 example) → [ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) — *agent: Opus 5 · medium (as built) — coordinator-scoped `triggered` channel closed once via sync.Once (idempotent, concurrency-safe); WaitForSignals selects over the signal channel and the trigger channel, so a Trigger arriving first latches rather than being lost; Register/Shutdown/WaitForSignals signatures unchanged*
- [x] 10.4 ratelimit.Middleware() + ErrLimited — 429-on-deny HTTP middleware over the existing engine (v2 item 8) → [ADR-0031](docs/adr/0031-ratelimit-middleware-design.md) — *agent: Opus 5 · medium (as built) — `(*Limiter).Middleware()` returns the house `func(http.Handler) http.Handler` decorator from the ratelimit package (spec §5 names it on the Limiter; net/http is stdlib so the L2 layer takes no new dependency); admits via Allow and never Wait, so a burst is shed rather than queued into parked goroutines; 429 + constant `Retry-After: ceil(1/rate)` precomputed per decorator, generic status-text body, no logging (a client-triggerable log line would be a flood amplifier); admit path 0 allocs; global-budget/per-client-fairness limitation documented (threat model, control C-5)*
- [x] 10.5 hash.HashPasswordCost (cost 10–31) + argon2id migration godoc note + cost-sizing benchmark (v2 item 20, §7) → [ADR-0032](docs/adr/0032-hash-password-cost-design.md) — *agent: Opus 5 · high (as built) — range validated LOCALLY, not delegated: bcrypt silently promotes sub-MinCost(4) values to the default and honours costs 4–9 verbatim (verified empirically, pinned by a test), so an out-of-range cost returns wrapped `ErrInvalidCost` and no hash; error not panic (unlike NewLimiter/Cors this has an error channel and runs per-call — `config.Load` precedent); `HashPassword` delegates at the default cost; adds `Cost()` so the mandated rehash-on-login note is actionable without importing bcrypt (the one deliberate widening of the item, flagged in the ADR); [cost-sizing report](docs/benchmarks/2026-07-26-hash-bcrypt-cost-sizing.md) shows exact doubling (55 ms→887 ms for cost 10→14) and that verify costs the same as hash — the per-login DoS trade-off, mitigated by 10.4's middleware (C-5); control C-4 extended + 3 threat-model rows*
- [x] 10.6 config.WithStructValidation() — wire validator.Struct into config.Load as an option (v2 item 13) → [ADR-0033](docs/adr/0033-config-struct-validation.md) — *agent: Opus 5 · low (as built) — opt-in Option (implicit enablement rejected: a struct with no tags passes vacuously, so it would imply a guarantee that isn't there); **tags run BEFORE Validator and a tag failure skips Validate**, so a Validate method may assume every field is individually well-formed and check only cross-field invariants; validator's panics on tag misuse/non-struct T are NOT softened into errors (that would override ADR-0023 from outside). **Establishes the module's FIRST internal package edge (config → validator, L2 → L2) — see 10.8***
- [x] 10.7 Fuzzing — FuzzConfigLoader + FuzzValidatorTags, committed corpora, CI fuzz job (10-min budget) (v2 §7) → [ADR-0034](docs/adr/0034-fuzzing-strategy.md) — *agent: Opus 5 · high (as built) — `FuzzValidatorTags` could not use the naive "never panics" property because `validator.Struct` **panics by contract** on tag misuse (ADR-0023); the invariant is instead "any panic is a `validator: `-prefixed string and **never a `runtime.Error`**, any error is a ValidationErrors" — which also catches a malformed tag being *silently accepted*. Tag injection needs `reflect.StructOf`, whose type cache **never evicts**, so the tag space is deliberately bounded to ≤3 fragments from a 16-entry table (unbounded tag text would OOM the runner, not fuzz better). Corpus is hand-authored around hostile documents (YAML alias bombs, 200-deep nesting, BOM, invalid UTF-8, NUL, `${` expansions) rather than committed mutation artifacts. **Found and fixed a real contract violation: `Load` promised the zero T on error but returned the partially decoded struct** — a half-configured value escaping behind an error. Ran clean: 1.65M execs (validator), ~51k (config, file-write bound)*
- [x] 10.8 Import-graph enforcement — depguard rules for the ADR-0004 allowlist (yaml.v3→config only, prometheus→metrics only) + go mod graph CI assertion (v2 §3) → [ADR-0035](docs/adr/0035-import-graph-enforcement.md) — *agent: Opus 5 · medium (as built) — enforced TWICE with different reach: depguard per file (each governed module confined to its owning package, driver/redis SDKs denied, sibling imports denied except config which gets a `strict` allowlist of stdlib+yaml+validator) **plus** `tools/import_graph_lint.py` over the resolved graph (direct go.mod requires vs the rings, per-package direct imports, internal edge set, `go mod graph` vs the manifest). The second exists because **depguard does NOT report a blank import of a sibling** (verified — `import _ ".../cache"` passes while blank `yaml.v3` is caught), cannot see a new direct requirement, and cannot notice a dead exception. The `config → validator` edge is allowlisted as an exception, and the tool fails if it ever *disappears*. Rejected pinning the full `go list -deps` closure (every upstream bump would fail CI for a decision nobody made). Each rule verified by deliberate violation.*
- [x] 10.9 CI coverage gate ≥ 85% (v2 §7; raises the §10 floor) → [ADR-0036](docs/adr/0036-coverage-gate.md) — *agent: Opus 5 · low (as built) — enforced **per package**, not as a module-wide average: with 16 of 21 packages at 100% the module figure sits ~99%, so a package could rot to 50% and the gate would still pass — it could not fail. Per package the floor binds the weakest. Measured low-water mark is **fanout 93.3%** (then fanin 95.7, pubsub 96.4, retry 97.7, ratelimit 98.1); the 8-point margin is deliberate slack, since a gate that fires on noise gets disabled. `tools/coverage_gate.py` + a `coverage` CI job; no-statement packages skipped, not counted as zero; verified by deliberate violation. Also discharges AGENTS.md §10's outstanding "finalized in an ADR" and corrects its stale "module floor 1.24" → 1.25*
- [x] 10.10 NFR benchmark suite — NFR-01/02/03/04/06 benches + benchstat methodology + nightly regression workflow (>10% flags) (v2 §5) → [ADR-0037](docs/adr/0037-nfr-benchmark-methodology.md), [report](docs/benchmarks/2026-07-26-nfr-suite.md) — *agent: Opus 5 · max (as built) — split by KIND of claim: hardware-independent NFRs are **hard gates** in the test suite (NFR-01 allocations, NFR-04 ±1% accuracy exact via the fake clock, NFR-05), hardware-dependent throughput/latency is **measured and reported**, not gated (shared runners move microbenchmarks >10% between identical runs; a gate that fires on noise gets switched off). **Verdicts: NFR-03 met 12.7x (6.33M delivered/s), NFR-02 throughput met 4.4x (4.42M tasks/s), NFR-04 met EXACTLY (0.0000%), NFR-05 already gated; NFR-01 latency met (938ns of 1µs) but its 0-alloc target is UNACHIEVABLE (8 allocs — context.WithValue + r.WithContext + Header.Set are structural), replaced by an enforced ratchet budget; NFR-06 NOT MET (Get 79ns but 90/10 mix 350ns — a single RWMutex serialises readers on every Set: this is 10.11's sharding evidence); NFR-02/06 p99 UNVERIFIED — on Windows 100% of adjacent time.Now/Since pairs read 0ns, so the suite reports `tail-unmeasurable` rather than clock artifacts.** Follow-up found: `middleware.HeaderName` is non-canonical (`X-Request-ID` vs Go's `X-Request-Id`), costing 2 allocs/request in CanonicalMIMEHeaderKey for no wire-format difference — left to the maintainer as an API-visible change*
- [x] 10.11 cache hardening — 1 000-cache create/close goleak test + NFR-06 1M-entry p99 bench; shard internally only if the bench demands (v2 item 17) → [ADR-0038](docs/adr/0038-cache-sharding.md) — *agent: Opus 5 · medium (as built) — **the bench demanded it**: 10.10 measured 349.8 ns for the 90/10 mix vs 78.9 ns read-only, all from one `sync.RWMutex` serialising readers behind every `Set`. Sharded into **32 independently locked shards** keyed by `maphash.Comparable` with a **per-cache seed**; **NFR06Mixed 349.8 → 46.6 ns (7.5x), now under the 200 ns target**. Uncontended ops pay ~5 ns for the hash (GetHit 27.2→32.9, GetMiss 15.1→20.1, Set 52.0→60.5) — the trade is recorded, not hidden. **ONE sweeper per cache preserved** (per-shard sweepers would be 32 000 goroutines for 1 000 caches) and now pinned by `TestThousandCachesOwnOneGoroutineEach`; thousand-cache create/use/close, concurrent lifecycle, and 3-way concurrent idempotent Close all goleak-clean. `removeExpired` sweeps shard-by-shard so no lock spans the keyspace — harmless because ADR-0021 has `Get` enforce expiry. No API change, no dependency change, 100% coverage*
- [x] 10.12 pubsub.WithDropOldest — additive slow-subscriber policy option (default stays drop-newest + handler) (v2 item 2) → [ADR-0039](docs/adr/0039-pubsub-drop-oldest.md) — *agent: Opus 5 · medium (as built) — opt-in broker Option; on a full buffer the **oldest buffered** message is evicted so the subscriber sees the freshest (state-like streams: gauges, price ticks, progress). **Best-effort BY CONSTRUCTION**: evicting from a channel is receive-then-send, and a subscriber or publisher can act between them — retrying has no bound and would break ADR-0006's "Publish never blocks", so a lost race degrades to drop-newest for that one message. **The drop handler reports the EVICTED message** (the one actually lost) — reporting the incoming one would count safely-arrived messages as drops and break the accounting. **Invariant preserved under both policies: per subscription every message is delivered or reported dropped exactly once** (NFR-03's benchmark asserts it). Saturated-path cost 74.9 → 138.4 ns/op (~63 ns, only where messages are already being lost); NFR-03 throughput unchanged at ~6.4M delivered/s. No-op for rendezvous subscriptions (nothing to evict). Rejected: retry-until-success (unbounded), a per-subscription lock (adds a lock to the hot path to fix a race whose only cost is which message drops), a per-subscription policy (Subscribe is frozen), an enum option. **pubsub 96.4% → 100% coverage** — the gap was the option validators' panic branches*
- [x] 10.13 contrib/ nested submodules — contrib/redishealth + contrib/pgxhealth (own go.mod, independent tags) supplying health.Check probes (v2 item 22 / ADR-003) → [ADR-0040](docs/adr/0040-contrib-submodules.md) — *agent: Opus 5 · max (as built) — each submodule **requires the released core (v1.0.0) with NO `replace` and NO `go.work`**: a `replace` is ignored for dependents so CI would validate a configuration no consumer gets, and a committed workspace would switch all eight root CI jobs into workspace mode for the sake of one frozen struct. Exported constructors take the **driver's own type** (a consumer shouldn't write an adapter) while internals use a one-method interface, so every branch is testable to 100% without a live server; each module also constructs a real client/pool to prove the exported signature accepts the concrete types (neither dials eagerly). **Verified the core is untouched:** root go.mod/go.sum unchanged, `go list ./...` returns no contrib package, `go list -deps ./...` contains no redis/jackc path. **Extended the three things that silently ignore a nested module:** `import_graph_lint.py` now fails if a contrib dir holds Go files without a go.mod (the silent failure — those files join the root module and drag the driver in while every `./...` check keeps passing) and runs FIRST since later checks otherwise die on an opaque `go list` error; `coverage_gate.py` measures 23 packages (21 core + 2 contrib, both 100%); CI grows a `contrib` matrix job (build, vet, -race, gofumpt, golangci-lint, govulncheck) and Dependabot an entry per submodule. Shared `contrib/.golangci.yml` shadows the root depguard, which — verified — forbids the driver import *and* the health import a contrib module exists to make. **Milestone 10 complete**; contrib modules are NOT released by the v1.1.0 tag*


---

## Milestone 11 — Governance: namespace contract & spec reconciliation — ✅ complete

Governance only: no code, no version bump, no consumer-visible change. The series' shared
identity is restated as the **logical namespace `it.d4np.utils.<component>`** rather than
ADR-0002's physical tree, which two of the three scaffolded sibling repositories had already
abandoned independently — each because its language binds names to paths differently. Go
realizes the namespace at the module root, so imports are unchanged.

The milestone also discharges the **regeneration caveat ADR-0003 left open**: the manifest that
the factory re-renders from still asserted that packages live under `src/main/go/it/d4np/utils`,
so a regeneration would have silently re-imposed the layout ADR-0003 removed.

Pulling that thread exposed a second, unrelated class of drift, which 11.2 closes: the **frozen v1
specification had diverged from the as-built project**. 11.2 closes the §3/§6 divergences: three
facts that had gone stale are amended in place, while §3's compatibility clause — a governance rule
that was *replaced* rather than drifted — takes an ADR instead
([ADR-0042](docs/adr/0042-post-1.0-compatibility-contract.md)). The spec now carries a dated
**Amendments** block so that no post-freeze edit is silent.

A full read-through of the spec then ran against the as-built API and found the divergence is wider
than §3/§6: **§5 does not list the public surface v1.1.0 shipped, and §4 states an architectural
invariant that 10.6 deliberately broke.** Those are scoped to **Milestone 12**, not retrofitted
here — see the journal for the enumerated findings.

> **Agent guidance:** Claude Opus 5 · effort **max** — a one-way, series-wide identity decision
> whose alternatives (vanity module path, GitHub organisation) are breaking and hard to unwind
> once published; the reasoning, not the diff, is the deliverable.

- [x] 11.1 Record the series logical namespace `it.d4np.utils.<component>` and close the ADR-0003
      regeneration caveat → [ADR-0041](docs/adr/0041-series-logical-namespace.md) — *agent: Opus 5 · max (as built) — the decision reframes ADR-0002's premise: a **physical** layout cannot be a cross-language contract, because in most of these languages the layout is not free (Maven dictates Java's, the include model dictates C/C++'s, the module system dictates Go's), whereas a **namespace** can be. Verified rather than assumed: `egl-util-cpp` keeps the tree, `egl-utils-c` rejected it in its own ADR-0002 (`d4np/<module>/` at the root), `egl-utils-java` is unscaffolded — so the tree was 1-for-3, not the norm. **Module path deliberately NOT changed**: the two options that would render the namespace literally in Go source (vanity `go.d4np.it/utils`, org `github.com/d4np/…`) are both module-*identity* changes and therefore breaking, and the vanity one converts a documentation preference into a permanent supply-chain obligation (a domain that must resolve and a `go-import` tag that must be served for as long as anyone runs `go get`). Recorded in the ADR-0030 ledger as declined-with-a-condition: **the move is free at a `/v2` boundary and only there**, since consumers rewrite every import at such a boundary anyway. `orchestrator/project.yaml` amended in place with a dated note (the interview record still shows what was asked; the generator can no longer act on it). **Two findings carried forward, neither fixed here:** the EADOS bundle's `go.yaml` profile still asserts the tree but `.eados-core/` is gitignored, so the correction must be carried upstream; and `egl-util-cpp` ships `it/d4np/util` (singular) against this contract's `utils` — a series-level call, not this repo's to make*
- [x] 11.2 Reconcile the frozen v1 spec §3/§6 with as-built reality — language floor, coverage gate, leak-detection mechanism, and the post-1.0 compatibility contract → [ADR-0042](docs/adr/0042-post-1.0-compatibility-contract.md) — *agent: Opus 5 · high (as built) — the sweep that began as "fix the stale 1.24 floor" turned up **four** divergences in a document declared frozen, and they are not the same kind of thing. Three were **facts that had drifted** and were amended in place under the spec's own divergence rule: the language floor (1.24 → 1.25, false since the M9/M10 dependency work), the coverage floor (80% → **85% per package** — "per package" is the operative half, since with most packages at 100% a module-wide average could never fail; stricter since 10.9/ADR-0036), and the §3/§6 hedge promising "an in-repo stack-based guard until ROADMAP 2.6 lands the test-only deps", which described a state that ended in M2. The fourth took the **ADR branch** instead: §3's compatibility clause made a breaking change *mergeable with a MAJOR-intent note*, while the v1.0.0 commitment makes it not mergeable into v1.x at all — a rule that was **replaced**, not a number that drifted, so the original text is **struck in place and retained** rather than rewritten, because overwriting it would erase the evidence that the promise to consumers ever changed. **ADR-0042 retires the MAJOR-intent note and makes ADR-0030 §2's ledger the only sanctioned destination for a breaking change** — the ledger stops being a filing cabinet and becomes the enforcement point. The frozen spec now carries a dated **Amendments** block that distinguishes the two kinds of post-freeze edit, so future divergences inherit the split. No code, tooling or CI change: the rule has been in force since v1.0.0 (bucket 2's seven deferred deltas are the proof) and is only now written down*

---

## Milestone 12 — Public-interface reconciliation — ✅ complete

The five divergences the M11 read-through found and did not fix. They were held back from 11.2
because they are a different size of change: §5 is a rewrite, and its closing line is a question
about the promise made to consumers rather than a stale number.

The milestone's real target is the **root cause**, which 12.3 addresses: nothing checks a spec
claim against reality. `consistency_lint.py`'s spec-map gate verifies that every section has a
fulfilling roadmap item — never that the section's *claims* are true. That is how nine divergences
accumulated across ten milestones without a single red build.

> **Agent guidance:** Claude Opus 5 · effort **high** — 12.1 touches the versioning surface, which
> is a consumer-facing promise; 12.3 is ordinary tool work and can drop a tier.

- [x] 12.1 §5 reconciled with the module's actual exported surface, and the versioning clause widened to bind it → *agent: Opus 5 · high (as built) — §5 had never been updated after Milestone 10: **twelve identifiers shipped in v1.1.0 were missing** (`(*Breaker).State` + the `State` type, `lifecycle.Trigger`, `(*Limiter).Middleware` + `ErrLimited`, `HashPasswordCost`/`Cost`/`ErrInvalidCost`, `config.WithStructValidation`, `pubsub.WithDropOldest`), plus pre-M10 omissions (`middleware.HeaderName`, `errors.StackTracer`, `validator.ValidationErrors`/`FieldError`, `logger.Field` + its five constructors, `workerpool.Task`, all thirteen `WithX` option constructors, and the root `Version`). **One listed signature was outright wrong** — `NewBroker[T](opts ...Option)`, where the real option type is generic `Option[T]`, so code written to the spec does not compile. Rebuilt from `go doc` output, not by hand. **The substantive half is the closing clause:** it read "SemVer over all exported identifiers **above**", which made the enumeration the boundary of the promise and left every unlisted identifier outside it — **narrower than the v1.0.0 changelog's "API stability for every exported identifier"**. It now binds the whole exported surface and says the list is a reader's map, not the boundary, with `go doc` as the authority and `contrib/*` excluded per ADR-0040. That **widens the spec to match what was already published** rather than changing the promise, which is why it is an amendment and not an ADR*
- [x] 12.2 §4's isolation claim superseded, and §6/§3 understatements corrected — *agent: Opus 5 · medium (as built) — §4's absolute is struck in place and superseded by **the existing [ADR-0033](docs/adr/0033-config-struct-validation.md)**, deliberately **without minting a new ADR**: that ADR already holds the decision, so a fourth governance record would add a pointer and no information. The replacement wording is not just "there is one exception" — it states that the edge is a **governed** exception, because `import_graph_lint.py` fails **in both directions**, when an unsanctioned edge appears *and* when `config → validator` disappears (ADR-0035), and the rule enforced is "same-layer edges only where the spec mandates the composition". Spec item 13 is what mandates this one. The isolation property is kept but qualified rather than dropped: every other package is still adoptable alone, and adopting `config` also brings `validator`, which costs nothing since both ship in this module. §6/§3 were understatements, not false claims: rapid was credited with three areas and runs in **eight** packages, benchmarks with four and exist in **seven**, and §3's dependency sentence omitted `prometheus/client_model` — a direct `go.mod` require that no non-test file imports. Corrected against `grep`/`go list` output, not from memory. **Milestone 12's remaining item, 12.3, is the reason these four kept accumulating** — none was caught by a gate*
- [x] 12.3 `tools/spec_api_lint.py` — fail the build when §5 and `go doc` disagree → [ADR-0043](docs/adr/0043-spec-api-lint.md) — *agent: Opus 5 · high (as built) — **it found a tenth divergence on its first run**: `workerpool.ErrPoolClosed`, exported, absent from §5, and missed by both Milestone 10 and the M11 read-through because it is the **second member of a `var (…)` block** whose first member, `ErrQueueFull`, was listed. That is also why the throwaway checker written during 12.1 was not good enough — it scanned column-zero declarations and reported **110** identifiers where this tool reports **130**; the twenty it could not see were block members, struct fields and interface methods. **A checker with a blind spot is worse than none, because it certifies the part it cannot see** — the 12.1 PR's "110 verified" claim was true as stated and weaker than it sounded. Authority is `go doc -all` (the pkg.go.dev rendering), not a Python re-implementation of Go's grammar: build tags, embedded types and grouped declarations each become a false negative in the one tool that must have none. **Fails in both directions** because they are different bugs — shipped-but-unlisted is how M10's twelve accumulated, listed-but-gone is a stale promise a consumer can compile against, and under ADR-0042 a removal is MAJOR-only. Reverse direction recognises functions, methods and sentinels (the shapes §5 writes unambiguously); a renamed type is caught by the forward direction instead, and the limit is documented rather than implied. **Verified by deliberate violation in all three shapes** (identifier removed from §5; fabricated §5 entry; new exported func with the spec untouched). Wired into the `imports` CI job, which already provisions Go. Also documented the **three** previously undocumented policy tools: `import_graph_lint.py` and `coverage_gate.py` had never reached AGENTS.md's tree, the quality-bar table, `local-build.md` or the PR template — the same drift class this milestone exists to close. **Milestone 12 complete***

---

## Milestone 13 — `/v2`: `pkg/` layout and the ledger emptied — ✅ complete (v2.0.0)

> **The milestone outlived its own release, by design.** 13.10 shipped `v2.0.0` and 13.9 migrated
> `contrib/*` onto it — ADR-0040 requires the *released* core, so 13.9 could not run until the tag
> existed. The ledger is empty and contrib followed the tag; the milestone closed there.

The module's second major. It exists because the twenty-one-package root had grown to **forty
entries** and the maintainer could no longer read it — and once a layout change was on the table,
every import had to change anyway, which is the definition of a major.

**Spending a major is the expensive part; spending it once is the whole point.**
[ADR-0042](docs/adr/0042-post-1.0-compatibility-contract.md) made
[ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) §2's ledger the only destination for a breaking
change, and [ADR-0041](docs/adr/0041-series-logical-namespace.md) recorded that deferred breakage is
free at a `/v2` boundary **and only there**. Seven items were waiting. A boundary opened and not
used has to be opened again, and a library that ships v3 six months after v2 is one nobody trusts —
so 13.2–13.8 empty the ledger in this same major. **They did: the ledger is empty as of v2.0.0**, and
ADR-0030 §2 records it as discharged rather than pending.

Each ledger item is a public API design decision with its own ADR, its own PR, and its own §5
amendment; `tools/spec_api_lint.py` (ADR-0043) fails the build if §5 and `go doc` disagree, so the
spec cannot drift behind the redesign. **13.9 must run after the `v2.0.0` tag**, not before:
ADR-0040 forbids a `replace` and requires the *released* core, so `contrib/*` cannot reference
`/v2` until it exists.

> **Agent guidance:** Claude Opus 5 · effort **max** — every item is a one-way change to a published
> API. 13.5 (pubsub reshape) and 13.6 (removing the Prometheus SDK from the surface) are redesigns,
> not renames, and carry the most risk.

- [x] 13.1 `pkg/` layout + the `/v2` module path → [ADR-0045](docs/adr/0045-pkg-layout-and-v2.md) — *agent: Opus 5 · max (as built) — **the Maven tree was built, verified green, and reverted**, which is how the decision got a number instead of an opinion: the working version showed the consumer import at **86 characters** against `pkg/`'s 59 and v1's 52, for a root listing indistinguishable from `pkg/`'s. The clutter was never caused by the packages being at the root, it was caused by there being twenty-one of them — any single parent fixes it, and only depth differs. **`/v2` is a module-path suffix, not a directory**; the tree is not duplicated and v1 stays reachable at its own tags, which is what lets contrib keep requiring the released v1 until 13.9. **The module root keeps its package** — doc.go/version.go/version_test.go stay beside go.mod, so `…/v2` is importable and pkg.go.dev has a landing page; the experiment that moved them left the module with no root package at all, legal and worse. Tooling needed more than a path edit: `import_graph_lint.py` built contrib paths as `MODULE + "/contrib/"`, which with a major suffix demands a module that does not exist, so **REPO is now separate from MODULE**, and a shared `short_pkg()` strips `SRC_ROOT` too, keeping the allowlists written in short names. **depguard's `config-imports-only-validator` allowlist named a concrete path and so rejected the one *sanctioned* edge after the move** — a red build that looked like a violation and was a rule gone blind. Both layers re-verified by deliberate violation; the first attempt used a *blank* import and depguard stayed silent, which ADR-0035 already documents as its known gap and is exactly why `import_graph_lint.py` exists — the repo's own record answered the false alarm. Root: 40 entries → 19*
- [x] 13.2 `errors` → `errx`, with opt-in `WithStack` / `[]Frame` (ledger, v2 item 25) → [ADR-0046](docs/adr/0046-errx-opt-in-stacks.md), supersedes ADR-0029 — *agent: Opus 5 · high (as built) — the rename was indeed the easy half. Stack capture is now **opt-in**: `Wrap`/`Wrapf` attach a message and never reach the runtime, `WithStack` captures and is idempotent. **ADR-0029's origin-pointing property survives but is now structural rather than maintained** — the stack lives at one node and `Frames` finds it by unwrapping, so wrapping cannot move a trace because wrapping never touches it. `Frame{Function, File, Line}` keeps program counters out of the surface, **resolved lazily under a `sync.Once`** because symbolization measured **6.5× the capture** (4147 ns vs 643) and most errors are never printed. `Frames(err)` replaces the four-line `errors.As` dance; `StackTracer` stays as the extension point it searches. **THE MEASUREMENT CHANGED THE ARGUMENT: v1's `Wrap` cost 276 ns even when it captured nothing**, because `originStack` ran an `errors.As` walk over the chain on every wrap to find the stack already there — so v2's `Wrap` is 14.8× faster on a stackless chain (550.1 → 37.1 ns, 3 → 1 allocs) **and 7.7× faster on the path v1 had already "optimised"**, its cost now independent of chain contents. Surface is six identifiers; a `Frame.String()` and a raw-PC accessor were both dropped as **additive later, so free to omit now**. 100% coverage, reached ADR-0029's way — by deleting a dead defensive branch, not by leaving it uncovered*
- [x] 13.3 `cache.Get` → `(V, bool)` plus a `New` alias (ledger, v2 item 17) → [ADR-0047](docs/adr/0047-cache-comma-ok.md), supersedes ADR-0021's `Get` signature — *agent: Opus 5 · medium (as built) — **`New` is a rename, not an alias**: the ROADMAP said "alias" but spec v2's own gap analysis said `New(...)`, and two constructors for one type is what a major removes rather than adds (maintainer confirmed). **`ErrNotFound` is deleted, not kept as a landmark** — nothing returns it, so `errors.Is(err, cache.ErrNotFound)` would still compile and simply never be true, turning a compile-time break into silent runtime falsehood. **ADR-0021 is only PARTIALLY superseded and that matters**: its lazy-expiry-on-Get model is what makes the boolean sound, since expiry is judged at call time and never against the sweeper, so there is no window where `Get` returns true for an entry the caller must not use — had expiry depended on the sweeper, the bool would have been a weaker promise than the error. Absence and expiry stay indistinguishable (they already were), because separating them would promise callers something about eviction timing that ADR-0038's per-shard sweep deliberately does not offer. **The one genuinely new invariant got its own test: a stored zero value is not a miss** — implicit under `(V, error)`, but the entire reason comma-ok exists, and without it every caller caching zeros or empty strings silently re-fetches forever. No behavioural change, no perf claim (returning a bool instead of a preallocated sentinel cannot move a measurement, and asserting a win would be manufacturing one), 100% coverage retained*
- [x] 13.4 `workerpool.Stop` → `Close` (ledger, v2 item 1) → [ADR-0048](docs/adr/0048-workerpool-close.md), supersedes two *names* in ADR-0005 — *agent: Opus 5 · low (as built) — the vocabulary sweep was run and found exactly one outlier: `cache.Close()`, `pubsub.Close()`, `workerpool.Stop(ctx) error` are the module's only three goroutine-owning shutdowns, so the item was correctly scoped. **`ErrPoolClosed` → `ErrClosed` was folded in**: the ROADMAP line named only the method, but ledger item 1 names both, and half-discharging an item inside the major that exists to empty the ledger is how a v3 gets scheduled (maintainer confirmed). **`Close` keeps its `ctx`, against the literal ledger target `Close() error`** — that table's own gap column flags the method and sentinel names and never the parameter, so `Close() error` is its shorthand, not a decision; dropping the context would force a `Close` that waits on caller-supplied tasks to choose between an unbounded wait and a hidden timeout, which ADR-0025 refused on purpose. **So v2's shutdown vocabulary is uniform on the verb and deliberately not on the signature**, and that asymmetry is principled: the pool is the only shutdown that waits for work the caller wrote, since a sweeper and a fan-out loop are the module's own goroutines and terminate by construction. The accepted cost is stated rather than discovered — **`*Pool` does not satisfy `io.Closer`**. No new test: the rename introduced no new invariant and the three lifecycle tests already pin the behaviour under the new spelling, so adding one would have been a duplicate that looked like diligence. No behavioural change, no perf claim, 100% coverage retained*
- [x] 13.5 pubsub reshape — ctx-scoped subscription, `Publish(ctx) error`, `ErrSlowSubscriber` (ledger, v2 item 2) → [ADR-0049](docs/adr/0049-pubsub-reshape.md), supersedes ADR-0006's two *signatures* and ADR-0039's option *name* — *agent: Opus 5 · max (as built) — the brief said decide what `error` means before writing any of it, and that was the whole item. **"Publish never blocks" survives, and stops being an accident of the signature**: ADR-0006 argued it *from* having neither a context nor an error to return, so the promise was previously unarguable and now has to be stated and defended. Both new parameters were given deliberately narrow meanings so they cannot erode it — **`ctx` is consulted exactly once, before anything is delivered**, which makes cancellation all-or-nothing rather than a partial fan-out and makes the context an admission check rather than a deadline; the **`error` reports what already happened and never that the publisher waited**. The reading that had to be foreclosed explicitly is the tempting one: a `Publish` that blocks or waits on `ctx` when a buffer is full converts every slow subscriber into backpressure on every publisher, which is the exact failure a non-blocking broker exists to prevent. `ErrSlowSubscriber` says *that* this publish lost something, not which — the drop handler answers "what was lost", the sentinel answers "did this publish lose anything", and a topic with no subscribers is **not** an error, because nobody listening is a normal state in publish-subscribe. **Cancelling the context is the unsubscribe**, so the returned `func()` is gone — a second lifetime nobody was obliged to manage, where every caller already held a context describing the lifetime they wanted. **`context.AfterFunc` is what kept ADR-0006's best property**: a goroutine per subscription would have been the obvious implementation and would have destroyed the zero-goroutine claim, which is now load-bearing in a second place. Two edge cases were collapsed into one — an already-cancelled context and a closed broker both return an **already-closed channel**, the one result `range` handles correctly without the caller testing for it. **Scope grew on the gap analysis's authority, not mine: it requires "drop-oldest *or disconnect*"**, so `WithDropOldest[T]()` became `WithSlowSubscriberPolicy[T]` over a three-valued enum — an enum rather than a second boolean because two booleans make "drop-oldest *and* disconnect" expressible and need a precedence rule the compiler cannot enforce. `DropNewest` is the zero value, so the default is unchanged. **`Disconnect` uses the buffer as its tolerance** rather than adding a second threshold to tune, leaves already-buffered messages receivable, and cannot be told from a normal shutdown — stated as a cost rather than solved with a second channel. **`topic` is kept against the ledger's literal `Subscribe(ctx, filter)`, on 13.4's tie-breaker**: the gap column flags the API *shape* and never mentions topics, and removing topic routing would be a feature deletion wearing a signature change's clothes, pushing routing into either the filter or one broker per topic. ADR-0039's best-effort caveat carries over verbatim and its reasoning is reproduced rather than referenced away, since it is load-bearing here. Surface 135 → 139 identifiers; **a caller who ignores the new error gets exactly v1's behaviour**, so the migration is two passes: make it compile, then decide where `ErrSlowSubscriber` matters. 100% coverage retained, with every new invariant pinned — including the two rendezvous cases that surprise, where `DropOldest` is a no-op and `Disconnect` kills on the first missed message*
- [x] 13.6 metrics — remove the Prometheus SDK and `prometheus.Registerer` from the public API (ledger, v2 item 23) → [ADR-0050](docs/adr/0050-metrics-without-the-sdk.md), supersedes ADR-0027's surface + implementation and ring 3's membership in ADR-0004 — *agent: Opus 5 · max (as built) — **the ROADMAP's own wording here was the narrower reading and lost**: "remove the SDK from the public API" would have been satisfied by hiding it behind an unexported registry, but the gap analysis says "**No Prometheus SDK** — emit exposition text format directly" and its gap column names two objections, "SDK in API + dependency". Third time the same tie-breaker has fired (13.3, 13.4, 13.5). **The dependency was nine modules, not one**: `go mod why` showed every one of perks, xxhash, goautoneg, prometheus/common, procfs, x/sys and protobuf was in the graph solely for `client_golang`, plus `client_model` as a direct test-only require — nine of eighteen, `go.sum` 50 lines → 24, and `x/sys` leaving **retires ADR-0027's accepted uncalled-advisory trade-off** outright. **The measurement that had to happen before deciding: `promhttp.Handler()` was serving 37 metric families nobody had chosen** (29 `go_*`, 6 `process_*`, 2 `promhttp_*`, and none of ours until traffic arrives) — verified, not assumed, and the maintainer signed off on losing them rather than hand-rolling a subset, because the dep was being imposed on every consumer to serve the few who scrape runtime metrics. **`New() *Recorder` is ADR-0031's shape, not a new one** (`(*Limiter).Middleware` got there first), and turning the registry parameter into ownership closed a wart ADR-0027 had recorded rather than wanted: `Prometheus(myReg)` + package-level `Handler()` served the *default* registry, so a custom one silently exposed the wrong metrics. Double-install stops being a panic and becomes two independent recorders. Package-level funcs over hidden state were rejected on ADR-0025's singleton costs — bought with nothing, since the signature had to change anyway. **Conformance is proved against the reference encoder and outlives it**: `testdata/exposition.golden` is `expfmt`'s own output for these two families, captured while the SDK was still a dependency, so a failure means the writer drifted from the *format*, not from an earlier version of itself — which is what pinned the four things nobody should re-derive by eye (families in sorted name order, labels alphabetical with `le` last, cumulative buckets, shortest-round-trip floats so `1` is `1`). A test-only Prometheus dep for ongoing validation was declined: four modules to re-verify a frozen format. **Recording is lock-free and a per-series mutex was rejected on ADR-0038's evidence** — most requests share one label pair, so that mutex would serialise the hot path; the accepted cost is a scrape that can see a histogram mid-update, made one-directional by writing the count last so `+Inf` is never below it. **Measured, both implementations in one binary: 223.0 → 63.4 ns and 1 alloc → 0** (the allocation isolated to `strconv.Itoa(status)` — keying the series on an int and rendering once per series is the whole trick), scrape 48.4 → 11.4 µs and **436 allocs → 3** after two optimizations measurement found rather than guessed. Advantage narrows to 1.5× under contention on one label pair — recorded, not tuned. ADR-0027's cardinality guarantees survive intact and the bound is now **our** arithmetic: at most 9 000 series. Both enforcement layers edited per ADR-0035, with the `$all`-no-`!$test` mechanism verified by deliberate violation using a real import. Surface 139 → 141, 100% coverage retained*
- [x] 13.7 `WaitForSignals(timeout, sigs...)` (ledger, v2 item 21) → [ADR-0051](docs/adr/0051-lifecycle-shutdown-timeout.md), supersedes TWO points of ADR-0025 and nothing else — *agent: Opus 5 · medium (as built) — the brief said overturn ADR-0025 explicitly rather than silently, and **reading what it actually argued is what made that tractable: its objections were narrower than its heading.** It refused a deadline that was *hidden*, *library-invented*, *default* and would *silently truncate* what the operator configured — every adjective describing a deadline **the caller did not choose**. A mandatory first parameter is none of them, so **the reasoning is preserved and only the conclusion reverses**; it is also what shaped the replacement. **`timeout == 0` imposes no deadline** (maintainer's call, recommended): byte-for-byte v1 behaviour, and not a compatibility shim but the *right* posture under systemd or Kubernetes, where a second number in the application duplicates a grace period configured one level up and the shorter silently wins — so ADR-0025's substantive claim stays alive as a documented option instead of being deleted. Forcing every caller to invent a number was rejected for exactly that reason, and a second `WaitForSignalsTimeout` function on 13.4's grounds (two entry points for one lifecycle is the ambiguity these items remove). Negative panics (ADR-0005). **Note the tie-breaker ran the other way here for the first time**: 13.4 and 13.5 kept `ctx` and `topic` because the gap column named only signatures, and this row names "bounded-deadline **philosophy**" as a gap in its own right. **The deadline is measured from the SIGNAL, not the call** — deriving it earlier would spend the process's whole uptime before shutdown began, so a service up for a day would get no grace period at all; pinned by a test and **verified by moving the derivation before the `select`, which fails it** (1.499s remaining against a required 1.6s). **The bound is cooperative and never abandons the sequence:** a hook honouring its context winds up early, one ignoring it still completes, and an expired deadline does not skip the remaining hooks, because ADR-0025's run-every-hook-and-join decision is untouched — the timeout bounds what *can* be bounded and only the platform can be a kill switch. Per-hook budgets stay deferred (ADR-0025 already deferred them), and `WaitForSignals` still returns nothing since adding a return is source-compatible for a statement call, hence additive and free to defer. No new dependency, no perf claim (one `context.WithTimeout` once per process is not a finding), surface unchanged at 141 — the signature changed, the identifier did not. 100% coverage retained with five new tests*
- [x] 13.8 bcrypt default cost 10 → 12 (ledger, v2 item 20) → [ADR-0052](docs/adr/0052-hash-default-cost-12.md), supersedes the *value* of ADR-0024's default cost and nothing else — *agent: Opus 5 · medium (as built) — **the last of the seven ledger items, and the only one where nothing in the surface moves**: no identifier, no signature, no compile error anywhere. The constant *is* the contract, which is what ADR-0042 says and what made this major-only — re-measured before flipping it at **×4.03** (57.7 → 232.3 ms per verify, same workstation as 10.5, ratio 2.00–2.03 per step still exact), so a consumer who upgraded in a minor would have found their login endpoint consuming four times the CPU with their code unchanged. **The capability was never the point — `HashPasswordCost(pw, 12)` has shipped since v1.1.0. The item is about what a caller who expresses no opinion gets**, and the mechanism is the gap between default and floor: `minCost` stays 10, so the strong factor is what you get by not choosing and cost 10 is still legal but has to be written down. Raising `minCost` to 12 was rejected — it would turn a default into a mandate against deployments that measured and chose 10, and push them to `x/crypto/bcrypt` directly where ADR-0032's silent-promotion footgun waits. Going to 13 or 14 was rejected as the same mistake mirrored: a library inventing a number for hardware it cannot see (ADR-0025's reasoning, preserved by ADR-0051). **The module also stops inheriting `bcrypt.DefaultCost`** — v1 read as deference to upstream but was an absent decision, since the factor protecting a consumer's password store was whatever a dependency bump last chose. **No stored hash is invalidated and no migration exists to run:** bcrypt encodes the factor, so v1's cost-10 hashes keep verifying and move up through rehash-on-login — pinned against a **captured** cost-10 hash rather than one the current code produces. Exporting `DefaultCost` was declined on ADR-0032's own surface-discipline grounds plus a new one: omission is additive and reversible, publishing a number this ADR expects to move again is not. The DoS trade-off is now inherited rather than opted into, so the godoc states ~4.3 logins/s per core **at `HashPassword` itself** and names `ratelimit.(*Limiter).Middleware` there. Second consecutive item `spec_api_lint` could not have caught; surface unchanged at 141. Control C-4 tightened, three threat-model rows amended, the 10.5 sizing report re-verified in place. The suite's own cost went 2.6 s → 6.3 s and the tests were **left on the public default path** — one that hashes at a factor no consumer gets is testing something else. 100% coverage retained with two new tests*
- [x] 13.9 migrate `contrib/*` to `/v2` — ran **after the `v2.0.0` tag**, so after 13.10; [ADR-0040](docs/adr/0040-contrib-submodules.md) **amended, not superseded** — *agent: Opus 5 · medium (as built) — both submodules now `require …/egl-utils-go/v2 v2.0.0` and import `…/v2/pkg/health`. **The item is four lines of import and two of `go.mod`; what it demonstrates is why it could not be one of the eight items inside the major.** ADR-0040's "require the *released* core" is not a style preference, and the block was concrete rather than theoretical: `go list -m …/egl-utils-go/v2@v2.0.0` answered **"unknown revision"** until the tag was pushed. The rejected `replace ../..` would have let this ship at any point during the major with CI green throughout — while the `require` line consumers actually resolve went unexercised, which is precisely the failure ADR-0040 refused. **So the constraint that delayed the item is the same one that makes it verified when it lands**, and the delay is a milestone that outlives its own release rather than a cost. **The migration used the `sed` snippet published in the v2.0.0 release notes — contrib is the guide's first real consumer, which is why matching the closing quote mattered**: a rewrite anchored on `…/egl-utils-go/<lowercase>` would have turned the `contrib/redishealth` self-reference into `…/v2/pkg/contrib/redishealth`. **One measured side effect invisible in the diff: MVS raised `pgxhealth`'s transitive `golang.org/x/sync` from v0.17.0 to v0.22.0**, because the core v2 requires the newer pin — a major bump moves a submodule's indirect graph even when its own code does not change. Module paths and tag scheme untouched (a contrib path carries its own major, not the core's); both modules 100% coverage, all four policy tools green, `go.mod`/`go.sum` of the core **provably unchanged**. **Deliberately not done here: tagging `contrib/*`** — ADR-0040 keeps their releases a separate act, and the version choice (v0.2.0 vs v1.0.0) is the maintainer's* — **followed through on 2026-08-01: both submodules are tagged `v0.2.0`**, a minor inside `v0` because no identifier changed while the returned `health.Check` type moved module, and the procedure (including that `release.yml` never fires for a `contrib/…` tag) is now in [release.md](docs/workflow/release.md); see the [checkpoint](docs/journal/2026/08/2026-08-01-post-v2-audit-and-contrib-tags.md)
- [x] 13.10 release v2.0.0 → [notes](docs/releases/v2.0.0.md), [changelog](docs/changelog/v2/v2.0.0.md) — *agent: Opus 5 · high (as built) — **ran before 13.9, which is the milestone's one ordering inversion and was planned**: ADR-0040 needs the *released* core, so the tag has to exist before contrib can reference it. `version.go` → 2.0.0, `[Unreleased]` **moved** into `docs/changelog/v2/v2.0.0.md` (a new per-major directory) **reordered from newest-first into roadmap order**, since 13.1's layout change is what every other entry's migration depends on; README badge + milestone table, release notes indexed, ADR-0030 §2 marked emptied. **The migration guide lives in the release notes rather than in a second document** — the ROADMAP line said the notes carry the import table, and a standalone guide duplicating seven API entries is two documents free to drift. **The `sed` snippet in those notes was RUN, not written**: the conventional `s|…|…|` form fails with "unknown option to `s`" because the package alternation itself contains `|`, and matching the closing quote is what keeps a `contrib/*` import — a separate module sharing the path prefix — out of the rewrite. Both verified against a file importing all four shapes. **Per-package coverage re-measured at the cut instead of trusted from the PRs**: all seven reshaped packages at 100%. The lockstep gate was verified live by reverting `version.go`. Carry-through per release.md §8: the agent tags and pushes after the merge, CI drafts the Release, the maintainer clicks Publish*

---

## Milestone 14 — Adoption: examples, the release act, and the supply chain — ✅ complete

The v2 surface is complete, specified and gated — 141 identifiers, four policy tools, twenty of
twenty-three packages at 100% coverage. **What is missing is everything around it.** A consumer
arriving at pkg.go.dev finds twenty-one packages and **one runnable example between them**. The
submodule release act, exercised for the first time on 2026-08-01, has **no machine watching it**:
`release.yml` matches `v*.*.*`, so a `contrib/…` tag runs no CI on the tree it tags. Three of CI's
actions float on a mutable tag while the rest are SHA-pinned — a supply chain half-secured, which
reads as a policy and is not one. There is no `CONTRIBUTING.md`. And roughly twenty "deferred,
additive" lines sit in sixteen ADRs, invisible unless you grep for the word.

**This milestone deliberately adds no exported identifier.** Every item closes a gap the project has
already recorded about itself, and the one thing it must not do is guess at capability nobody asked
for — each of those deferrals was argued as *additive later, so free to omit now*, which is a promise
to wait for a consumer, not a to-do list. 14.10 gives that promise a ledger, the way
[ADR-0030](docs/adr/0030-spec-v2-reconciliation.md) §2 gave the breaking deltas one, so **the
milestone after this one is chosen from demand rather than invented.**

No spec section changes hands, so the Spec Coverage Map is untouched. It still ends in a release:
examples are `_test.go` code no consumer compiles, but **pkg.go.dev renders them from the tagged
tree**, so they reach a consumer only when a version exists — that is 14.12's whole argument.

> **Agent guidance:** Claude Opus 5 · effort **medium** — most items are documentation, CI and
> convention, with no API risk and no behaviour to break. The decisions live in 14.2 (the examples
> convention), 14.6/14.7 (the release act and signing) and 14.10 (the ledger); 14.5 repeats
> ADR-0040's module-boundary reasoning, where getting it wrong silently drags a dependency into the
> core's graph while every `./...` check still passes.

- [x] 14.1 open Milestone 14 — the ROADMAP section, the README row, the GitHub milestone — *agent: Opus 5 · low (as built) — planning only, no code, on 10.1's precedent that a milestone opens in its own PR so the plan is reviewable before any of it is built. The theme was a genuine choice rather than a discharge: with ADR-0030's ledger empty there was nothing to schedule, and the two rejected alternatives are worth recording — **implementing the ~20 deferred additive items** (rejected: it contradicts the argument every one of those ADRs made for deferring, and 14.10 turns it into a demand-driven ledger instead) and **observability/integrations, an otel-shaped tracing layer plus more contrib probes** (rejected for now: it grows surface and the contrib line before the existing product's adoption gaps are closed, and otel's dependency tree needs ADR-0004 reopened, which is a decision that deserves its own milestone rather than a corner of this one). `consistency_lint`'s `milestones` check compares the README row against the section's checkboxes, so the row goes in as 🚧 while any item is unchecked*
- [x] 14.2 the examples convention + the concurrency and resilience set → [ADR-0053](docs/adr/0053-runnable-examples-convention.md) — *agent: Opus 5 · high (as built) — **13 examples across the eight packages, and the convention really was the item.** Four rules, each recorded with the reason it exists. The **external** test package, because an in-package example compiles against unexported state and so nothing would stop it documenting a call a consumer cannot write — and it turned out to be **an existing pattern made explicit rather than a new one: 20 of 21 packages already carry an external test file**, `lifecycle` the lone exception and 14.3's problem. A **verified `// Output:` on every example**, because the trap is silent: **`go test` compiles an `Example` with no output comment and never runs it**, so it proves only that the code builds while looking identical in a green run — `go test -v` was used to confirm all thirteen actually execute instead of trusting a package-level `ok`. **Printed shape, never strings**: `validator.ExampleStruct` already printed `err != nil` rather than the error text, and generalising that is what stops a reworded error message from breaking documentation — booleans, counts, sorted slices and status codes only, which is also why `fanin` sorts before printing and `fanout` prints a total instead of which output won. **And no example may depend on the clock** — a rule that was forced rather than chosen: the fake clocks in cache/circuitbreaker/ratelimit/retry are **unexported fields**, so rule 1 puts them out of reach, and exporting a seam was refused twice over (M14 promises no new identifier, and a knob whose only caller is documentation ships forever). The replacement beats the seam: a **one-minute** breaker timeout makes the lazy half-open transition provably unable to fire mid-example, `BaseDelay: 0` makes retries immediate (ADR-0011 already defines it that way), the limiter's bucket **starts full** so burst-then-refuse needs no refill, `workerpool`'s queue-full path is reached by waiting on a `started` channel the example owns — an **ordering** claim, not a timing one — and `semaphore`'s bound is shown by handing capacity between goroutines, where the example terminating *is* the proof that the blocked acquire completed only after the release. `time.Sleep` appears nowhere: in a module built to remove goroutine-timing bugs, an example that sleeps in order to work teaches the habit the library exists to replace. Two traps avoided in passing: pubsub's **16-message default subscriber buffer** is what lets a sequential publish-then-drain example work at all (ADR-0049 — `Publish` never waits, so an unbuffered subscription would have dropped every message and the example would have hung on the first read), and `fmt.Println` of an **empty header value leaves a trailing space** that the example comparator does not trim per line, which is why the middleware example issues two explicit requests instead of looping. **Examples are not leak-gated** — goleak runs per test and there is no `TestMain` — so each one closes its pool, closes its broker and cancels its subscription context, which is what a consumer should copy in any case. No API change, no behaviour change, no dependency: 0 golangci-lint issues, gofumpt clean, four policy tools green, coverage unmoved at the gate's weakest point. The accepted cost is recorded rather than hidden: **a behaviour that is nondeterministic by nature can be described but not shown**, so cache TTL stays prose godoc in 14.4*
- [x] 14.3 examples: the HTTP and observability set — *agent: Opus 5 · medium (as built) — **12 examples across middleware, health, metrics, logger and lifecycle, under ADR-0053 with nothing re-decided.** The chain order got a **package-level `Example`** of its own rather than a sentence, because it is the thing most likely to be copied wrong: Recoverer outermost, then RequestID so an id exists before anything logs, then Logger, then Cors nearest the handler. Rule 3 (print shape, not strings) did the most work here — `slog` and the Logger middleware both emit a moving `time` and `duration`, so those examples **decode the JSON line and print only the stable fields**, which incidentally documents the schema better than dumping it would; the generated request id is random, so its example prints `!= ""` for the generated case and a **client-supplied** id for the deterministic one; and `metrics` filters the scrape to the counter family, since the histogram's buckets depend on how long the requests really took. **Two examples assert a security property rather than a happy path**, which is where an example earns more than prose: Recoverer's prints `strings.Contains(body, "hunter2") == false` against a panic message holding a password, and health's shows a 503 body naming *which* check failed and never *why* — an unauthenticated caller gets no hostnames or DSNs. **`lifecycle` was the hard case the ADR named in advance, and the answer was to write less:** `WaitForSignals` blocks until a signal that will never arrive, so it appears as prose *inside* the example — with the timeout argument and what `0` means — while the runnable part is `Register` + `Shutdown` proving LIFO order. That package is also the module's only **process-wide singleton**, and this example shuts the real one down: safe only because all 21 internal tests swap the coordinator behind an unexported seam, which an external example cannot reach — so the file carries a **note telling the next contributor to keep it the only example here**, and the coupling was checked with `-shuffle` on four seeds rather than assumed. Two globals are mutated and restored (`slog.Default`, for Recoverer's log sink and `FromContext`'s base logger), which the full suite plus shuffled runs confirm does not leak into the neighbouring tests. revive caught the one thing worth catching — an unused `ctx` in a health probe — and the fix was to make the probe **honour** the context (`return ctx.Err()`) instead of hiding the parameter, since a probe that ignores ctx is the bug the example should not model. 0 golangci-lint issues, gofumpt clean, whole module green, four policy tools green; no surface, behaviour or dependency change*
- [x] 14.4 examples: config, storage, validation and the core set — *agent: Opus 5 · medium (as built) — **29 examples across the eight packages, and the module is now complete at 55 across all 21** — every exported surface a consumer meets has a call site that executes. ADR-0053 held again, with the three named traps each answered rather than worked around. **The `db` decision went to the stub connector, and the argument is that ADR-0004 was never the obstacle it looked like**: `database/sql/driver` is *standard library*, so a ~35-line stub adds no dependency, no `go.sum` line and nothing for `import_graph_lint` to object to, while prose-only godoc would have left the module's most safety-critical helper as the one thing a reader cannot see run. What the stub buys is precisely what prose cannot state: it counts commits and rollbacks, so the three examples **assert the finalization contract** — commit 1/rollback 0, commit 0/rollback 1, and the panic path showing `recover()` receiving the original value with the rollback already done. That third one is the reason the helper exists, and it is the case a hand-written `defer` gets wrong. The stub is deliberately its own, not the `fakeConn` `db_test.go` already owns: reuse would have saved 35 lines and put `newDB(conn)` in the rendered example body, where a reader sees only the function, not the file. **`hash` was budgeted, not avoided**: the whole package's example set does five bcrypt operations and measures **0.74 s** — `ExampleHashPassword` spends 0.67 s on the register-then-login triple at the default cost, and `ExampleCost` uses an explicit cost 10 because that is honestly what a legacy store contains, not to save time. The 72-byte and out-of-range-cost examples cost nothing at all: both are rejected before any work happens. **`config` writes into a fresh `os.MkdirTemp` per example rather than a shared `os.TempDir()` path**, which keeps them independent under `-shuffle` and parallel runs. gosec caught the finding worth keeping — `G101` on `cfg.DSN == "postgres://app:s3cret@localhost/app"` — and the fix was not a `//nolint`: the example now prints `strings.Contains(dsn, "${")` and `strings.Contains(dsn, "s3cret")`, which is both a *better* demonstration of what expansion did and one less credential-shaped literal in the documentation. Two rule-3 judgements are recorded because they look like violations: `errx` **does** print `"handling GET /profile: loading user: not found"`, legitimately, because every word of it is the example's own — the sentinel and both wrap messages — and pins no `errx` wording; and `errx.Frames` is asserted with `strings.HasSuffix(frames[0].Function, "loadConfig")`, which proves the origin-pointing property ADR-0046 made structural without pinning a path or a line. `validator.ExampleStruct` **moved** out of `validator_test.go` into the conventional `example_test.go` and grew three siblings, so the one pre-existing example now lives where the ADR says examples live. Coverage: all eight packages were **already at 100%** and stayed there — examples document, tests verify, and none of these stands in for a missing test. 0 golangci-lint issues, gofumpt clean, whole module green including shuffled runs, four policy tools green; no exported identifier, behaviour or dependency change*
- [x] 14.5 `examples/service` — one runnable service composing the module, as a module of its own → [ADR-0054](docs/adr/0054-examples-service-module.md) — *agent: Opus 5 · high (as built) — **the silent-failure claim was verified before it was designed around, and it is worse here than in `contrib`.** With `examples/service/go.mod` moved aside, `go list ./...` reports the package *and* `go list -deps ./...` **still succeeds** — because everything a composition example imports is already in the core's graph, so there is nothing to fail on. A contrib directory losing its `go.mod` at least dies on the driver import; here the boundary can vanish with **no error of any kind**, and the first sign would be an unexplained line in the library's own `go.mod` after someone's `go mod tidy`. **depguard is a second net that fires accurately and diagnoses wrongly** — with the file hidden it reports every `…/v2/pkg/…` import as an unsanctioned internal edge, "feature packages do not import each other", a rule nobody violated, and its suggested fix is to change the imports rather than restore the boundary; that is the argument for short-circuiting on the cause. **The lint check was generalized rather than copied** (`NESTED_MODULE_PARENTS` over `contrib` + `examples`) and now walks **recursively**, closing a hole the old top-level listing had: Go files only under `cmd/server/` with no `go.mod` above them are the same failure one directory deeper. **Scope grew on the item's own wording — "no `replace`, no `go.work`" was prose in ADR-0040 and had been unenforced since 2026-07-27**, so the tool now also refuses a `replace`, a committed workspace, and a core requirement on a pseudo-version, for contrib as well. All five new failures were confirmed **by deliberate violation**, not by reading the code. **A new CI job rather than an `examples/service` entry on the contrib matrix, for a reason outside the YAML: `contrib / redishealth` and `contrib / pgxhealth` are two of master's thirteen required status checks, and a renamed required context does not fail a PR — it blocks it forever, waiting.** **THE COVERAGE DECISION IS NO FLOOR, and the measurement was taken before deciding rather than to justify it:** the module is at **56.2%** because `main()` is **17 of its 48 statements** and none are reachable from a test (it binds a port and blocks in `WaitForSignals` on a process-wide singleton), while `service.go` — every decision the module exists to show — is at **87.1%** and clears the 85% floor on its own. Including it would mean lowering the library's floor for a demo, or adding a second per-module threshold to a tool whose entire argument is one floor applied per package. What replaces it is the bar that actually matters for documentation: CI **runs** it, `go test -race` included, because a directory of Go files that only compiles proves nothing — ADR-0053 rule 2's mechanism, transferred. **Twelve tests assert every claim the comments make**, so the pointed ones are gates rather than prose: the limiter refuses the second order while `/healthz`, `/readyz` and `/metrics` answer five times each; the 429 appears in the exposition and the scrape does not; with the pool closed `/healthz` is 200 while `/readyz` is 503 **and names which check failed, never why**; a single worker survives a panicking task and runs the next one. **The readiness probe exercises the real admission path** — it submits a no-op through the same `Submit` the handler uses, so the pool's own three errors are already the three answers an orchestrator needs; a probe returning `nil` documents wiring and verifies nothing. Eight packages, not twenty-one (a demo importing everything makes every package look mandatory and teaches nothing about composition), `env` over `config.Load` (a demo whose first step is "write a YAML file" is one the reader must set up first), and **stdlib-only tests: the `go.mod` has one `require` line and no indirect requirements at all, `go list -deps .` resolves 203 packages of which zero are third-party** — ADR-0004's policy made visible by the smallest program that can. One demand data point for 14.10, found by writing a consumer rather than reading the surface: **`env` has no float getter**, so the rate limit is an `int` converted at the call site. Core `go.mod`/`go.sum` provably untouched, surface unchanged at 141, 0 golangci-lint issues, gofumpt clean, four policy tools green. **Open for the maintainer: `examples / service` is not yet a required status check** — adding a job does not add the context, and until it does a PR can merge with the job red*
- [x] 14.6 `contrib-release.yml` — verify and draft on a `contrib/**/v*` tag → [ADR-0055](docs/adr/0055-contrib-release-workflow.md) — *agent: Opus 5 · medium (as built) — **the item asked for two decisions and the second one reversed the item's own title: it verifies and drafts nothing.** (a) The trigger is `tags: ["contrib/*/v*.*.*"]`, which works because a GitHub ref filter's `*` does not match `/` — so the two release workflows **partition** the tag space instead of overlapping on it, and the core's `v2.0.0` cannot reach this file. The parse from `github.ref_name` to a module directory really is the job rather than a preamble to it, for a reason worth stating plainly: **a wrong answer would not fail, it would `go build ./...` in the repository root, pass, and report a green submodule release for a module nobody touched.** Five refusals, each exiting non-zero with the value it rejected — one path segment under `contrib/`; a SemVer remainder; **a `go.mod` in that directory**, printing the module directories that *do* exist at that commit, because the realistic cause is a misspelling and the useful output is the list to spell it from; a `module` line equal to `…/egl-utils-go/<dir>`; and the **`/vN` suffix Go requires from v2**, which is the one a hand-written tag gets wrong and which the proxy would otherwise refuse *after* the push. The leading character class in the directory regex is load-bearing: it is what rejects `contrib/../v1.0.0`, whose `go.mod` test resolves to the root module and whose only other objection would have been the path comparison. All eight cases — two real tags, a legal `v1.0.0`, a legal pre-release, and the four failures — were run rather than read. **The sixth check is the one that could not be re-run: `git merge-base --is-ancestor $GITHUB_SHA origin/master`.** `ci.yml` runs on every push to `master` and `contrib / <module>` is a required context there, so reachability is the mechanical form of "the contrib CI job green on the commit being tagged" — a claim about *gating*, which no amount of re-running commands on a tag can establish. And **ancestry is the right test here even though the 2026-08-01 audit proved it the wrong test for a merged branch**: squash merges leave no ancestor for the branch, but the commit being tagged is a commit on `master`. Given that, the job draws a line rather than copying the CI steps: **it repeats what would make the released module broken for a consumer — `go build`, `go vet`, `go test -race`, `go mod verify` — and not what would make it merely untidy.** gofumpt, golangci-lint and govulncheck are properties of the tree, already gated per pull request, and a formatting finding is not a reason to unpublish a version; the build/test repeat is kept anyway, redundant in the ordinary case, so a tag's verification is self-contained instead of an inference across branch-protection state that lives outside the repository. `go mod tidy -diff` was refused here although the `examples / service` job runs it: that module has one requirement and no indirect ones, so a diff means a mistake, while a `contrib` module carries a driver's full indirect closure where a diff can come from tidy behaviour — failing a *release* on that is a stop with no correct action behind it. **(b) No GitHub Release, and the argument is not discoverability, which a Release would genuinely improve — it is that an unpublished tag is a recoverable mistake and a published Release is not.** Verification necessarily happens *after* the push, so this workflow's only possible output is "this tag is wrong", and the only response is to remove it — which `release.md`'s boundary already permits for an unpublished tag whose release run visibly failed. Drafting a Release would attach a human Publish checkpoint to the very artifact whose deletability is the remedy. Two consequences fall out for free: the workflow needs **`contents: read`** where `release.yml` needs `contents: write`, which is 14.7's principle arriving early; and it cannot manufacture the asymmetry 14.11 has to clean up — `v0.1.0`'s tag has no Release precisely because a Publish step was skipped once, and adding one to the module line that releases most often (driver bumps, Dependabot's cadence, not a milestone's) is a machine for making more of those. Auto-generated notes were a second strike: for a submodule tag they summarise everything merged since the previous `contrib/<name>/…` tag, which is mostly the core's unrelated work, where the hand-written annotated tag message says more in one line. What replaces the Release as a record is the run: `$GITHUB_STEP_SUMMARY` carries the tag, module path, version, directory and commit, plus the `go list -m <path>@<version>` line to confirm what a consumer resolves — **left manual on purpose, because the module index and the proxy see a new tag on their own schedule and asserting it in the job would fail on lag rather than on correctness, which is how a release gate teaches people to re-run red jobs.** `release.md`'s contrib section is rewritten from a hand procedure into a description of the gate plus the delete-and-repush remedy; the boundary table gains a CI row and no human one. Accepted and stated rather than hidden: **the gate is after the fact**, so a bad tag is caught, not prevented — bounded because nothing is published, and the irreversible case, a correct-looking tag the index has already served, is what the five parse checks make unreachable. The workflow **has never run**, since both existing submodule tags predate it and workflows are not retroactive; the first real exercise is the next `contrib/*` release. One adjacent defect noticed and deliberately left: `release.yml`'s `setup-go` and `ci.yml`'s `benchmark` job both read `${{ matrix.toolchain == 'go-1.25' && '1.25' \|\| '1.26' }}` in jobs that have no matrix — harmless, both evaluate to `1.26`, and 14.7 edits every step of both files, so a one-line cleanup inside a `contrib` PR is a change a reviewer has no reason to expect. No Go file, exported identifier, behaviour or dependency changed; four policy tools green*
- [x] 14.7 build-time supply chain: pin every action, least-privilege `permissions:`, SBOM + provenance on the release → [ADR-0056](docs/adr/0056-build-time-supply-chain.md), new control C-6 — *agent: Opus 5 · high (as built) — **three sweeps and two decisions, and each one's measurement disagreed with the line describing it.** (1) The half-pinning was **copy-paste, not policy**: the item blames three actions, but 21 of 36 references floated and **`actions/checkout` was among them** — unpinned at `ci.yml:80` and `ci.yml:289` in the same file that pinned it at eleven other sites, with both `actions/cache` subactions floating unmentioned. A repository where three actions float looks like three decisions; one where the *same* action is pinned eleven times and floating twice is a step pasted into a job that came later — the identical failure mode as the two no-matrix `go-version` expressions this item also fixes, which is the argument for a gate over a sweep, since a sweep only fixes today's copies. All 37 references now carry a 40-hex digest **and** a `# vX.Y.Z` comment, both enforced; the freeze-the-actions objection was already answered before this item and never stated — Dependabot has watched `github-actions` weekly since generation, and that entry is now the one dependency this decision creates and cannot itself detect. (2) **Least-privilege was three-quarters done and undocumented**: three of four workflows already declared `contents: read`, and the single real exposure was `release.yml`'s **workflow-level** `contents: write`, inherited by any job added later. So the rule shipped is stricter than narrowing it — **no workflow grants anything (`permissions: {}`) and each of the thirteen jobs asks** — which makes the defect unexpressible rather than fixed, and matters precisely because of the copy-paste pattern above; `ci.yml`'s `workflow_call:` is the second reason, a caller's cap being otherwise the only thing between a caller and eleven jobs holding write. Nothing else needed a scope and that was checked rather than granted defensively (`golangci-lint-action` wants no `pull-requests:` without `only-new-issues`; `upload-artifact` v4+ and the `cache` endpoints use the runtime service, not `GITHUB_TOKEN`). (3) **Every SBOM flag was decided against output.** `-type library` because the default `application` is false in the first field a scanner reads; **no `-test`, which yields exactly three components — `x/crypto`, `x/sync`, `yaml.v3`, ADR-0004's runtime set, the same count `import_graph_lint.py` independently reports** — because a library's consumer links three and never links testify, so the complete graph would overstate by 167% what anyone resolves; `-noserial -notimestamp`, **verified byte-identical across runs** (4 063 bytes) and now gated by `cmp` on every PR, so "reproducible" is a property and not a sentence; and **`-licenses` reversed on the measurement — it is wrong on all three**, calling `x/crypto`/`x/sync` `BSD-Source-Code` when their LICENSE files carry all three Go clauses including "Neither the name of Google LLC", and `yaml.v3` `Apache-2.0` when its own LICENSE opens "covered by two different licenses: MIT and Apache". CycloneDX reports detection as *evidence* because it is not guaranteed, but a compliance tool reads the field and not the hedge. **The provenance subject is the SBOM, not the module, and that restraint is the item's real decision**: what a consumer resolves is anchored in `sum.golang.org` (`h1:fg0qkFnLtGTVZK7QA63aWQi/+KMoEEscgNjMURXTy9k=`, the value in all three in-repo consumers' `go.sum`) with the proxy separately recording the version's origin, so a re-pointed tag is detectable by anyone who ever resolved it and an attestation over the module would stand a weaker claim beside a stronger one. What genuinely lacked an origin story is the **asset set, because it was empty** — all four releases have zero assets, so `release.md` step 10 had been false since v0.1.0. The module zip was considered and is unavailable: obtaining it means asking the proxy, and ADR-0055 forbids a release job asserting what the proxy has seen. Generation also runs per PR in the existing `imports` job (**no rename**, so no required context moves), because ADR-0055 had to admit its own workflow "has never run and cannot have". (4) **The gate extends `consistency_lint.py` to ten checks rather than becoming a fifth tool** — `consistency / lint` is already required, and ADR-0054's trap is that a new job is not a required context until someone edits branch protection by hand; it also keeps "all four policy tools" true in `AGENTS.md` §10, the PR template and nine journal files. Verified by deliberate violation in **ten** cases, and **the tenth was a hole in the new check itself**: `workflow-permissions` anchored its scope pattern to end-of-line while the only write-holding job documents each of its three scopes with a trailing comment, so it matched none of them, computed an empty write set, and **passed green on the single job it exists to police** with its allowlist entry inert. Found by asking what the check *saw* rather than whether it passed — ADR-0043's 12.1 lesson arriving somewhere new, that a checker with a blind spot is worse than none because it certifies what it cannot see. (5) **The signing premise in this very line was false.** `68fd847`, `b8c1165` and `51b9310` are each `verified=true`, committer **GitHub** — the repository is squash-only, so GitHub creates and signs the squash commit and an agent's unsigned branch commits never land on `master` as themselves. So the decision splits: `required_signatures` is **free** and recommended, while signed **tags** are declined, since tag creation is the agent's carry-through and a tag is not what a consumer verifies; recorded positively in the register rather than by omission, and `github-setup.md` now carries the project's first written word about signing. Also: **`orchestrator/project.yaml` amended in place** — its templates carried `setup-go@v5`/`checkout@v6`/`golangci-lint-action@v6`, no permissions blocks and the source of both matrix expressions, so its convention's test ("would a regeneration produce something wrong?") answered yes against a **live policy** for the first time rather than a stale fact. Accepted and stated: the attest-and-upload path **cannot run until a tag exists**, so 14.12 first exercises it; `contrib/*` gets no SBOM (no Release to attach to, and a 90-day artifact is not a record — a 14.10 candidate); `zizmor` declined, leaving template injection ungated as a class though the current pipeline was hand-checked clean (`contrib-release.yml` reads `$GITHUB_REF_NAME`, never `${{ }}` into `run:`). Third trust boundary added to the threat model with six STRIDE rows — the first new boundary since 4.1. Surface unchanged at 141, `go.mod`/`go.sum` provably untouched, no Go file changed, four policy tools green*
- [x] 14.8 the two unverified tails, measured on Linux CI → [ADR-0037](docs/adr/0037-nfr-benchmark-methodology.md) amended, [report](docs/benchmarks/2026-07-26-nfr-suite.md) updated in place — *agent: Opus 5 · medium (as built) — **both premises in the line below were false, and the second one cost the item its comfortable answer.** There was no job to add: `nfr-nightly.yml` had run the full suite on `ubuntu-24.04` at `-count 10` since 10.10, tail benchmarks included, `clock-res-ns` at 29–30 and `observed-tick-ns` at 10–20 — **eleven consecutive nights of published percentiles nobody had opened**, because they went into the uploaded artifact while the job summary carried only the benchstat table. That generalises past benchmarks and is 14.7's lesson one layer out: **a job that runs is not a number that has been read.** The fix is a `grep` into `$GITHUB_STEP_SUMMARY` *and* the job log — both, deliberately, since a summary is not reachable from `gh run view --log` — plus a warning when a tail comes back `tail-unmeasurable` on a clock that should manage it: not a perf gate (ADR-0037 forbids one) but a *refusal to publish*, which is the measurement going blind and hardware-independent in kind. **NFR-02's `Submit` p99 is MET at 176 ns against 2 µs — 11×** — and conservatively, since the benchmark's own comment claimed a queue "deep enough that Submit never waits on a worker" and `ThroughputCounted` refutes it: an atomic inside the *task body* slows **submission** from 106 to 144 ns/op, which cannot happen to a producer that never waits on the queue. (The tail also moved 176→255 ns between two runs of the same commit on the same image, so `Submit`'s distribution varies by runner instance — a standing argument for never gating on it.) **NFR-06's p99 is NOT MET, and publishing the measured figure would have repeated the Windows-tick mistake in a new costume:** the 8-goroutine tail reads 1604 ns against 200 ns, and the same benchmark line refutes it — aggregate **97.1 ns/op** with a p50 batch mean of **743**, which is 97.1 × 8. A wall-clock batch timed from *inside* one of N goroutines on M cores with N > M measures **residency**, not service time. So it is published as a residency time with the arithmetic that reads it, and `BenchmarkNFR06GetTailPerCore` runs the identical mix at one goroutine per core where N = M restores a service time (117.3 × 4 = 469 against a p50 of 455 — the identity checked at a second point). **That is where the expected answer died:** the service-time p99 is **887 ns**, so the shortfall survives with scheduling removed entirely and the spec's 8-core reference machine would not close 4.4×. The residual blocker is not hardware but what the target means — `GetHit` uncontended is 32.9 ns and inside 200 ns, while the same `Get` under the 8-way mix occupies ~775 ns of its caller's wall clock. Which also reads backwards onto 10.11's **"met at the mean, 46.6 ns", an aggregate *throughput* figure compared against a *latency* target**; ADR-0038's sharding result stands, the latency reading does not. Flagged for the maintainer as a spec question (ADR-0030 §3 is the register), deliberately not decided here. ADR-0037's split is intact — measured and reported, never gated*
- [x] 14.9 `CONTRIBUTING.md` + `CODE_OF_CONDUCT.md` — *agent: Opus 5 · low (as built) — **the item asked for a real contact and that is where the only genuine decision was, because the obvious answer is a decoy.** Every commit in this repository carries `106583643+danielPoloWork@users.noreply.github.com`, so it is already public, already the maintainer's, and costs no new exposure — and it is **the one address that cannot be used**, because `users.noreply.github.com` accepts no incoming mail. A contact that looks real and silently discards reports is worse than an obviously missing one: the reporter believes they have reported. The maintainer chose the repository's **private security-advisory form**, which is the only private authenticated channel here, so the Covenant's enforcement section points at it — and the mismatch is **designated in writing rather than left to look like a copy-paste error**, in the Code of Conduct, in the issue chooser, and in `SECURITY.md` from the receiving end, so an incoming conduct report is not triaged as a misfiled vulnerability. The one gap that channel cannot cover is stated where it matters: a report about the maintainer routes to GitHub Support instead, because an escalation path running through the person complained about is not one. **`CONTRIBUTING.md` is written against what a contributor gets wrong, not as a table of contents for `AGENTS.md`.** The `gofumpt` warning is the clearest case — CI pins `v0.10.0`, `@latest` gives `v0.11.0`, the two disagree about arguments on the opening line of a multi-line call, and the failure arrives as a red `quality` job on a diff the contributor never wrote; this cost a red CI round in 14.5 and is now the one paragraph in the file with a blockquote around it. Two more of the same kind: `gofumpt` ignores module boundaries, so a file under `examples/` or `contrib/` turns the **core's** job red, and the nested modules need their commands run from their own directory because a root `./...` never reaches them. The one-PR-at-a-time rule is given its **mechanism** rather than its instruction — a squash merge leaves the merged branch as no ancestor of `master` (the fact the 2026-08-01 audit established by finding 63 stale branches that `git merge-base --is-ancestor` called unmerged), so a stacked branch carries commits that no longer exist upstream and `strict` branch protection makes the rebase mandatory rather than optional. **§7 is the item's other half and it deliberately refuses a feature-request template**: the four questions it asks are answerable as evidence — what you need, what you do instead today, why here rather than in your own code or a `contrib/` module, what it would break — because the workaround is what shows the gap is real, and 14.10's ledger schedules on a **trigger**, which an issue thread does not produce. It routes to Discussions and says the ledger is not written yet rather than linking a file that does not exist. Also corrected, since the community-health file set was open anyway and the statement was false: **`SECURITY.md` promised support for `0.x` "until `egl-utils-go` reaches `v1.0.0`"** — untrue since v1.0.0 and misleading since v2.0.0 — now the latest `v2.x`, with `v1.1.1` recorded as permanently resolvable from the proxy and unsupported, which is the honest shape of a Go deprecation. `AGENTS.md` §7 gains the coupling as a rule (a gate, label, pinned version or ADR trigger changes both files in the same PR, this contract remaining the source of truth), because the failure mode for a second document restating the first is drift, and the README's `AGENTS.md` row stopped claiming to serve humans now that something else does. No ADR: the milestone's ADR numbers are spoken for and **0057 is reserved for 14.10** — this item adopts no pattern and reverses no decision, it writes down gates that already existed. Verified rather than assumed: every internal link resolved against the tree (ADR-0004 is `0004-runtime-dependency-policy.md`, not the name that reads naturally), and the claim that new files carry an SPDX header was **dropped after checking — 0 of 116 Go files have one**, only the Python tools do. No Go file, exported identifier, behaviour or dependency changed; four policy tools green*
- [x] 14.10 the additive-capability ledger → [ADR-0057](docs/adr/0057-additive-capability-ledger.md) — *agent: Opus 5 · high (as built) — **the census refuted both of the line below's numbers, and the second refutation is the one that would have shipped a wrong document.** Not "roughly twenty capabilities in sixteen ADRs": **49 open capabilities drawn from 26 ADRs**, plus one entry with no ADR at all. And **eleven deferrals had already been discharged without the deferring ADR ever being updated to say so** — ADR-0021 deferred `cache` sharding and 10.11 built it (ADR-0038); ADR-0024 deferred a configurable cost and a cost-upgrade helper and 10.5 shipped both (ADR-0032); ADR-0029 deferred "a bespoke frame type" and `errx.Frame` has existed since 13.2. **So a ledger assembled the obvious way — grep `deferred`, transcribe what it finds — would have proposed as future work capability the module already exports.** Each entry was therefore checked against the *source*, not against the ADR that deferred it, which is also how §D found that **two entries are not additive at all**: `*Pool` satisfying `io.Closer` needs a `Close` without a context (ADR-0048 states the cost), and argon2id *as the default* is the bcrypt-cost decision again with a different constant (ADR-0052) — both reclassified to ADR-0030 §2, which this ADR deliberately does **not** write into, since §2 is discharged and populating it on the strength of an unasked-for capability reopens a closed record. **The trigger column is the item and it is defined as falsifiable evidence** — what must be observed, by whom — over six kinds (`consumer`, `second-consumer`, `in-repo`, `measurement`, `external`, `paired`), the kind recorded because it names who is expected to report it. **`in-repo` is not theoretical: exactly one trigger in §A has fired**, `env`'s missing float getter, found in 14.5 by *writing* `examples/service` rather than by reading the surface — so the honest reading is that a milestone built from §A today would be one entry long, which is what "wait for a consumer" looks like and is the ledger working rather than disappointing. **Two tables, not one** (§A public surface, §B internal/process), because a merged trigger column would mean "demand from outside" in one half and "an event the maintainer notices" in the other, and that ambiguity runs the wrong way — it would license firing a surface trigger by wanting to. **Decision 4 is the rule that keeps it honest:** an entry must name the shape that makes it additive or it belongs in §2; "argon2id" is not an entry, "an additive `HashPasswordArgon2`" is. **Gated rather than left as prose, because prose in a Consequences section is exactly what no gate can see** — `consistency_lint.py` grows an eleventh check, `ledger-coverage`, asserting both directions between the canonical `Deferred, additive:` marker and the ledger, verified by deliberate violation *and* by printing the sets it sees (13 marked against 36 cited, neither empty — a check with empty inputs passes vacuously, which is 14.7's `workflow-permissions` hole). **Its blind spot is documented rather than discovered:** the marker covers 13 of the 26 ADRs, the other 13 defer in prose no pattern matches without false positives (`a deferred recover()`, "Go's `defer` intuition" and "deferring a nil-pointer dereference" all appear and none is a deferral), and retrofitting the marker into accepted records to please a regex was rejected — §A is complete by census, and the marker is a tripwire for the *next* deferral, where it costs nothing. Rejected alternatives worth not re-litigating: a living document under `docs/` (better on immutability, lost on symmetry — ADR-0042 already sends breaking changes to an ADR section, and two destinations are holdable where three are not), a `Priority` column (the backlog in disguise: priority is a claim about the future by someone with no evidence, the trigger is a claim about evidence), an issue per entry (49 open issues arguing against capability would misrepresent the project), and a lint check that each trigger is non-empty (theatre — any string passes, and "when someone needs it" is precisely the entry this ADR refuses). Coupled documents updated in the same PR per the rule 14.9 added to `AGENTS.md` §7: `CONTRIBUTING.md` §7 stopped pointing at an unwritten file and now tells a proposer to read the ledger first, the ADR template carries the marker and the additive-shape test, and ADR-0056 §(d) is amended in place from ten checks to eleven. **No exported identifier, no Go file, no behaviour and no dependency changed** — and the one fired trigger is deliberately not implemented here, because discharging an entry in the PR that creates the register would make the register look like a to-do list on its first day*
- [x] 14.11 repository metadata, the doc site, and the `v0.1.0` Release → [ADR-0058](docs/adr/0058-no-documentation-site.md) — *agent: Opus 5 · low (as built) — **the line called them "three unapplied `github-setup.md` §4 items" and §4 documented none of them; what §4 did document, and what nobody had noticed was off, was worse than all three.** Description, topics and homepage were empty for the project's whole life because **the setup guide never mentioned them** — undocumented setup is unapplied setup, and the fix is a new §4.1 as much as it is a `PATCH`. All three are now set, the description being the README's own one-liner so the two cannot drift into different claims, and the homepage pointing at `pkg.go.dev` — which is itself §4.3's decision made visible from the front page. **THE FINDING: private vulnerability reporting was DISABLED (`{"enabled":false}`), and it is load-bearing for TWO policies.** `SECURITY.md` names that form as the way to report a vulnerability and 14.9's `CODE_OF_CONDUCT.md` designates the *same* form for conduct reports, chosen over a published email precisely because it was "the only private authenticated channel here" — and with the setting off it is reachable only by someone who can already create a draft advisory, so **the entire audience for both documents had no route at all**. That is 14.9's own lesson arriving one layer out, and it was found by checking the setting rather than the prose: **a channel two documents point at is a claim about configuration, and configuration outside the repo is what no gate can see** (the 2026-08-01 governance-drift lesson, third instance after the labels and the merge strategy). Enabled on the maintainer's call and verified by readback; `github-setup.md` §4.4 now carries it as required rather than as a web-UI footnote. **Pages was costed before deciding, and the measurement decided it:** 160 Markdown files, 549 links that stay inside `docs/`, **30 across 12 files that escape it** — seven to `AGENTS.md`, three to `ROADMAP.md`, one each to `CONTRIBUTING.md` and `CODE_OF_CONDUCT.md` — and since Pages publishes *only* `docs/`, those are root files by convention that **cannot be served at all**. So the site's dead links would be exactly the governance documents a reader of ADRs clicks through to, which is why the decision is **not to publish** and not "publish and fix the links": rewriting them to absolute URLs makes 30 links *worse* in the repository, because an absolute `blob/master` link reads the wrong file from every branch and fork. The other two arguments are recorded so this is not re-litigated on aesthetics — **a Go library's doc site is `pkg.go.dev`**, which already renders 55 verified runnable examples for the consumer audience, while `docs/` serves contributors who are already in the repository with a file tree, blame and search; and a site is a **deploy surface** that under ADR-0056 needs a pinned, permissioned, Dependabot-tracked workflow to publish text that already renders one click away. 0 files use mermaid, so nothing was rejected on rendering. **A curated subset site — the genuinely best version — is not dismissed but registered**, ADR-0057 §B with a trigger, which is the new ledger's first real exercise: ADR-0058 carries the `Deferred, additive:` marker, so `consistency_lint`'s `ledger-coverage` check *required* the row, and the mechanism was confirmed by re-reading what the check saw (0058 present in both the marked and cited sets) rather than by a green run. **`v0.1.0`'s Release is drafted from `docs/releases/v0.1.0.md`** — the tag has existed since 2026-07-12 with no Release while four later tags each had one — **with `--latest=false`, which is the trap**: `gh release create` marks a new Release "Latest" by default, so backfilling an older tag moves the badge off `v2.0.0` and onto a version predating every feature package, visible to every visitor immediately. Verified: latest is still `v2.0.0`. Left as a **draft** per AGENTS.md §6.1 — the agent drafts, the maintainer publishes — and the procedure plus the flag is now in `release.md`. ADR-0055, which predicted this asymmetry as "the hole 14.11 has to clean up", is amended in place. **Followed up 2026-08-08: the maintainer chose NOT to publish that draft and it was removed, so this item's third deliverable did not land and `v0.1.0` still has a tag and no Release** — a legitimate call (the version predates every feature package and its own notes say not to install it, so the Release would have published little beyond "do not use this"), and the asymmetry is now a recorded decision rather than an oversight, which is the part that actually differs from the starting state. The other two deliverables — the metadata and ADR-0058 — are unaffected; `release.md`'s backfill procedure survives with a "decide whether it is worth publishing before drafting it" clause in front of it. No Go file, exported identifier, behaviour or dependency changed*
- [x] 14.12 release v2.0.1 → [notes](docs/releases/v2.0.1.md), [changelog](docs/changelog/v2/v2.0.1.md) — *agent: Opus 5 · medium (as built) — **the release whose entire payload is a tag.** `version.go` → 2.0.1, `[Unreleased]` **moved** into `docs/changelog/v2/v2.0.1.md` and **reordered from newest-first into roadmap order** (v2.0.0's convention, and here it matters more than usual: the milestone's ten items only make sense read as one argument about adoption), README badge + milestone row, notes indexed, `[Unreleased]` reset to the empty skeleton. **The notes lead with "no code changed" and prove it rather than asserting it** — no `.go` file under `pkg/` touched in the whole milestone, surface unchanged at 141, `go.mod`/`go.sum` byte-identical to v2.0.0's — because a consumer reading "v2.0.1" asks one question first and deserves the answer in the first line, not after a feature list. **The case for releasing at all is the tag itself:** pkg.go.dev renders from the *tagged tree*, so 55 runnable examples across 21 packages had been in `master` for a week and invisible to everyone who does not clone; and the release **artifacts** changed, so this is the first tag on which 14.7's SBOM-and-attestation path does anything — all four earlier releases have zero assets, which is what made `release.md` step 10 false since v0.1.0. **The lockstep gate did its job on the first run**: flipping the README milestone row to ✅ before 14.12's own checkbox failed `consistency_lint`'s `milestones` check, which is exactly the drift it exists to catch and a reminder that the last item of a milestone is the one most easily left unticked. Two findings are carried into the notes rather than buried in the changelog, because a consumer should not have to read a changelog to learn them: **NFR-06 is not met** (887 ns against 200 ns, nothing regressed — the target had never been verified, and 10.11's "met at the mean" compared throughput against latency), and **private vulnerability reporting was disabled**, so anyone who tried to report privately and failed is told plainly that it is fixed. The notes close on the ledger rather than on a feature list: **M15 is deliberately unchosen**, 49 deferred capabilities each with a trigger and exactly one fired, so the next milestone comes from demand — which is what M14 was for. Carry-through per release.md §8: the agent tags and pushes after the merge, CI drafts the Release and attaches the SBOM, the maintainer clicks Publish*

---

## Spec Coverage Map

Tracks which spec section is fulfilled by which roadmap item(s). Every spec section has a
row with at least one fulfilling item and a status glyph. Legend: ⏳ not started · 🚧 in
progress · ✅ done · ❎ N/A.

| Spec § | Requirement | Roadmap items | Status |
|--------|-------------|---------------|--------|
| §1 | Objective & business context | 1.1; delivered by M2–M9 | ✅ |
| §2 | Functional requirements | 2.1–9.5 (all 25 features) | ✅ |
| §3 | Non-functional requirements | 1.3, 1.4 (gates live); per-feature from M2; 11.2 (ADR-0042), 12.2 | ✅ |
| §4 | Logical architecture | 1.1, 1.6 (ADR-0003); 11.1 (ADR-0041); 12.2 (ADR-0033) | ✅ |
| §5 | Public interface | 2.1–9.5; 12.1 (surface + versioning clause) | ✅ |
| §6 | Verification & test strategy | 1.2, 1.4 (framework + CI live); per-feature suites M2–M9; 11.2, 12.2, 12.3 (ADR-0043) | ✅ |
