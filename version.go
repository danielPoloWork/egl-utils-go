package utils

// Version is the module's current Semantic Version. It is the source of truth
// the README status badge and release tags must stay in lockstep with
// (enforced by tools/consistency_lint.py; release protocol in AGENTS.md §11).
const Version = "0.1.0"
