# v0.1.0 — Milestone 1: project bootstrap & CI

**Date:** 2026-07-12 · **Milestone:** M1 — Project bootstrap & CI

The thinnest slice that compiles, tests, and ships under the full quality bar. No feature
packages yet — this release establishes the module, the gates, and the governance that
every subsequent release builds on.

## Highlights

- **Go module** `github.com/danielPoloWork/egl-utils-go` — idiomatic root layout
  (ADR-0003), language floor Go 1.24, CI on Go 1.25 & 1.26 across Linux/Windows/macOS.
- **Quality gates live and green**: build matrix, gofumpt, golangci-lint v2 (defaults +
  revive + gosec), `go test -race`, govulncheck, benchmark job, cross-artifact consistency
  lint.
- **Governance in force**: enterprise posture with the compliance register opened (C-1,
  runtime dependency policy per ADR-0004); per-milestone agent guidance (model × effort)
  in the roadmap.

## Verification

All 8 CI checks green on PR #6 — the first fully-green run of the matrix.

## Next

Milestone 2 — concurrency primitives: workerpool, pubsub, fanin, fanout, semaphore.
