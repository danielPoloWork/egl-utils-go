# v0.1.0 — 2026-07-12

Entries moved from `[Unreleased]` at release time
([Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/)). Milestone:
**M1 — Project bootstrap & CI**.

## Added

- Project bootstrap: enterprise scaffolding generated by EADOS (agent contract, docs system,
  CI gate, consistency lint).
- Go module bootstrap: `go.mod` (module `github.com/danielPoloWork/egl-utils-go`, language
  floor 1.24), root package `utils` with the `Version` constant, a SemVer smoke test, and
  the golangci-lint v2 config (defaults + revive + gosec).
- ADR-0003 — idiomatic Go root layout (supersedes ADR-0002 for this repository).
- ADR-0004 — runtime dependency policy; registered as compliance control C-1.
- Per-milestone agent guidance (model × effort) in `ROADMAP.md`.
