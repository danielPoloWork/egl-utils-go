# Build outputs
/build/
/dist/
/out/
/target/
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.a
*.lib

# Dependency / package manager caches
/node_modules/
/.venv/
/venv/
__pycache__/
*.pyc
/.gradle/
/vendor/
/.cargo/

# Coverage & test artifacts
/coverage/
*.gcda
*.gcno
*.profraw
.coverage
htmlcov/

# IDE / editor
/.idea/
/.vscode/
*.swp
.DS_Store

# Tool caches
/.mypy_cache/
/.ruff_cache/
/.pytest_cache/

# Generated docs
/docs/_site/
/docs/doxygen/html/

# Local agent / harness scratch (never commit)
.questions
.bugs
# Harness-local: session settings and the EADOS slash-command bundle copied in beside them —
# tooling that drives this repo, not source belonging to it (same reasoning as /.eados-core/).
.claude/

# The maintainer's authoring area for the spec and its drafts. What the project is held to is the
# copy imported verbatim under docs/specs/; these are the working originals.
/.spec/
/d4np-go.md

# EADOS factory tooling — the .eados-core/ bundle is copied in to (re)generate this repo; it is
# not part of the project's own source.
/.eados-core/
